-- Drop the orphaned function generate_weekly_nutrition_goals(UUID, DATE).
--
-- History: introduced in the original Studio-era nutrition system (restored
-- to migration 042 for chain-replay consistency). It read per-client target
-- columns (baseline_calories, calorie_target, custom_*_g, protein_target_g,
-- carb_target_g, fat_target_g, custom_macros_enabled, etc.) directly from
-- the `clients` row and wrote nutrition_daily_goals + nutrition_weekly_summaries
-- with a hardcoded Mon/Tue/Wed/Fri/Sat training pattern.
--
-- Why it's dead: superseded in migration 044 by the nutrition_plans +
-- nutrition_plan_daily_targets system (and later nutrition_events). All of
-- its input columns on `clients` were dropped in migration 045, so calling it
-- now would error with "column does not exist." Zero application-code
-- references; the only mention outside this drop is the autogenerated entry
-- in types/database.ts.
DROP FUNCTION IF EXISTS generate_weekly_nutrition_goals(UUID, DATE);
