# Measurement log — one store for every body measurement

**Status: commits 1–8, 8b and 8bb SHIPPED — `55ed1242` (2026-09-02), `29456d71` (2026-09-02), `24a2f667` (2026-09-03), `1872eeeb` (2026-09-03), `e8c58cad` (2026-09-03), `6ced90de` (2026-09-03), `83a0f061` (2026-09-03), `d4641a02` + `4e4b7009` (2026-09-03), `2f5390f4` (2026-09-03), `6870c595` + `e3802f45` (2026-09-04); commits 1–6, 8 and 8bb smoked, commit 7's fold reverted by commit 8, commit 8b's smoke owed; migrations 158–163 on DEV and PROD; the old stores are gone; a reading is edited in place, removed and restored; a logged day is derived once; the log is the selected metric's, and the metric is `?metric=`; the wellness series is a route over the client's daily log. Commit 8 made a reading EDITED IN PLACE (owner decision 2026-09-03, reversing the correction rows of D1 and D9 and the fold of commit 7 and D21: edit replaces, add adds, editing a check-in's reading edits its own row so the check-in follows, delete deactivates with Restore; D23: the day's value is the reading written last — an edit changes a value and nothing else, never a day's order or value, owner revision at execution). Commit 8b (2026-09-03) made the check-in review read its own day — the reading then, the goal version in force then read from the versions alone (a check-in older than every version shows no goal), the clock then, the ten check-ins up to it, the nutrition version covering its day, `goalIsCurrent` on the wire, and the AI prompt's previous-check-ins block bounded the same way. Commit 8bb (2026-09-04) made the nutrition deficit run from the day the plan takes effect — the drawer's Starts on, the preview and the save computing from it — and dropped the goal start date. Next: commit 8c, then 8cc. Commits 8c–8d and 9–11 are planned (8c a submitted check-in closes its period, 8cc the nutrition generation horizon runs as far as the training does — added 2026-09-04, lettered, independent of 8c and 8d, 8d a goal lives once and resolves by date — both added 2026-09-03 with letters; 8d amended 2026-09-04: goal versions are date-ranged like nutrition's, a goal may be queued to a future day and is invisible until it, replace only, the check-in review judges against the version covering its day, the drawer reads the version covering its Starts on, no view, D25 re-decided, D28–D29 added and answered, and the block target goes as a third commit under the letter; a row-per-goal shape with separate deadlines was considered the same day and declined: one deadline serves weight and body fat, and the other measurements are not goals) (8b — the check-in review reads its own day — was added 2026-09-03 with a letter, so nothing renumbers again); D1–D23 are answered (D17 moot; D18 reversed — coach wellness logging is retired, commit 10). Commit 12, the day-spine planning step, runs LAST because it ends by deleting this document (renumbered three times on 2026-09-03: the day-spine step went from 6 to 9 and 7–9 became 6–8; then the retirement of coach wellness logging and the client app's wellness series were inserted as 9 and 10 and the day-spine step became 11; then edit-in-place was inserted as 8, the wellness commits became 9–11 and the day-spine step 12).** Thirteen commits (the fourth conditional on D9; the fifth independent of the rest; the sixth the log's metric filter; the seventh the wellness series over the client's daily log; the eighth a reading edited in place; 8b the check-in review as of its day; the ninth the Wellness pane on that series; the tenth the retirement of coach wellness logging; the eleventh the client app's wellness series through the same kernel; the twelfth a planning step, not a build, and the last to run), each with a pasteable prompt (§6), each independently revertable in order, each gated. Commit 1 stands on its own: it fixed the goal strip on main.

**Scope:** the seven physique measurements — weight, body fat, waist, hips, chest, arms, thighs — and where they are stored, written and read. **Not in scope:** wellness (mood, energy, sleep, stress, soreness stay on the daily-logs model), the check-in form the client sees, the review page's layout, goals themselves, the RN wire shapes (they must not change). *Commit 8c (added 2026-09-03) touches the logs behind a check-in, not the form: a submitted check-in closes its period to further logging. Commit 8bb (added 2026-09-03) touches the nutrition drawer's effective date and the goal's start date, not measurements: the deficit runs from the day the plan takes effect, and the goal start date goes.* *Commit 8d (amended 2026-09-04) touches the goal's storage and liveness, not measurements: a goal is a date-ranged version, queued to a future day if the coach chooses, resolved by date everywhere.*

**How this plan is used.** Each commit's prompt tells a fresh session to read `CONVENTIONS.md`, `docs/ARCHITECTURE.md` and this file, and to **flag any rule in those two docs that contradicts the target shape in §2 rather than silently follow or silently override it**. Both docs describe the current three-store shape in places; a contradiction is expected, and the owner reviews each one. When a commit ships, the session replaces that commit's STATUS line in §6.

---

## 1. Why the shape changes

One concept — how much does this client weigh, how big is their waist — lives in three stores plus two caches, with copying rules between them. Every reader picks a store, and readers disagree.

| Store | What it holds | Written by | Read by |
|---|---|---|---|
| `check_ins.weight / body_fat_percentage / waist / hips / chest / arms / thighs` | the report's copy of what the client typed | check-in submit | review page (band, AI prompt, comparison), client app (check-in list, detail, progress series), coach Journey (merged) |
| `body_metrics` (mig 061) | immutable events, **weight and body fat only**, sources check_in / metrics_api / intake_sync / nutrition_plan / coach_entry | check-in submit (dual-write), coach entries (dual-write), intake, coach metrics route, nutrition plan generation (a TDEE provenance event) | comparison-service (starting-value fallback), nutrition-calc-inputs (rescue when the profile pair is null) |
| `client_metric_entries` (mig 132) | coach-logged values, all twelve metric keys, **replace-per-day** (unique client+metric+date), canonical units since mig 141 | coach entry route, the start pair on details-sheet save | coach Journey (merged with check-in columns client-side, `utils/metric-points.ts`, tie-break "coach entry wins the day"), Overview progression chart (`services/measurement-series-service.ts`, the same merge server-side), activity feed |
| `clients.current_weight / current_body_fat_percentage` | cache of the latest reading | check-in submit, coach entries (only when dated on/after the latest event), intake, coach metrics route | Overview status band, goal chips, goal strip (after commit 1), energy calculator, nutrition builder, drift note, intake, profile completeness, blocks |
| `clients.starting_weight / starting_body_fat_percentage` | cache of the start pair | details sheet, activation | comparison-service, client portal, Overview band |

Where it bit, all verified on DEV on 2026-09-02:

- **The goal strip said "No goals" for a client with a goal.** The check-in had no weight typed (every field is optional since the customisable form, mig 157). The comparison service only built a goal row from the check-in's own weight column, so the wire had no row, and the strip read "no row" as "no goal". The client's weight (75 kg) was on the cache the whole time; the Overview, reading the cache, said "5 kg to go" at the same moment.
- **A client who weighs in daily in the app and skips the box on Sunday's form** has a complete history in `body_metrics` and nothing on the review page.
- **A coach's correction does not reach the client.** A mistyped 90 cm waist on a check-in, corrected to 80 on the Journey page: the Journey shows 80 by tie-break, the client's own progress screen reads the check-in column and still shows 90.
- **Phantom history.** Every details-sheet save re-seeds the start pair through the entries upsert, which dual-writes a `body_metrics` event with no "unchanged" check. Two goal edits with the start weight untouched appended four identical immutable events in one minute.
- **Girths have no home outside a check-in.** Nothing captures them at intake; a client cannot log one between check-ins.

The ledger has carried the fix since March: TECHNICAL-DEBT → "Client Metrics Log Extraction". Migration 132 built the coach-entry half.

## 2. The target shape

**One table, `client_measurements`.** One row per recorded value: who, when, which measurement, the number, and where it came from.

```sql
CREATE TABLE public.client_measurements (
  id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  client_id    uuid NOT NULL REFERENCES public.clients(id) ON DELETE CASCADE,
  metric_key   text NOT NULL CHECK (metric_key IN ('weight','bodyFat','waist','hips','chest','arms','thighs')),
  value        numeric NOT NULL CHECK (value > 0),           -- canonical kg / cm / % (CONVENTIONS §20)
  recorded_on  date NOT NULL,                                 -- the day the reading belongs to (client-local)
  recorded_at  timestamptz NOT NULL DEFAULT now(),            -- when it was written; the day's value is the latest of these
  measured_at  timestamptz,                                   -- when the reading was TAKEN (D10); null = day known, time unknown
  source       text NOT NULL CHECK (source IN ('check_in','coach_entry','client_log','intake')),
  source_id    uuid,                                          -- the check-in id for source = check_in; null otherwise
  note         text,
  created_by   uuid,                                          -- auth user for coach entries
  created_at   timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX client_measurements_series_idx
  ON public.client_measurements (client_id, metric_key, recorded_on DESC, recorded_at DESC);
CREATE INDEX client_measurements_source_idx
  ON public.client_measurements (source_id) WHERE source_id IS NOT NULL;
```

**Rules of the shape** (each one exists to delete a rule that exists today):

*Rules 1–3 are SUPERSEDED for edits by commit 8 (owner, 2026-09-03): a reading is edited IN PLACE — edit replaces the row's value, add adds a row, editing a check-in's reading edits its own row with its stamp — and the day's value is the reading written last — an edit changes a value and nothing else, never a day's order or value (owner revision at execution, 2026-09-03) (D23). What survives of rule 1: nothing deletes a row, and every UPDATE the table sees is one of three RPCs (update, void, restore).*

1. **Append-only.** The app role holds `INSERT` and `SELECT` only; no `UPDATE`, no `DELETE`. A correction is a new row on the same day. History is never rewritten and never silently duplicated (rule 3). *D9 (§3) proposes the one exception: a reading can be REMOVED and RESTORED — an UPDATE that sets or clears `voided_at` and nothing else, through one RPC family. A removed row is invisible to every calculation and to the client, shown muted on the coach's list, and stays in the history; under D9, "latest row" in rule 2 reads "latest live row".*
2. **The value for a day is the latest row for that client, metric and day, by `recorded_at`.** One rule, no source ranking, no tie-break table. The coach correcting after the check-in wins; that is the case that matters.
3. **Writers append only on change.** A writer that would append a row equal in value to the day's current value for the same source does not write. This is what ends the phantom duplicates.
4. **Two caches remain, and only two:** `clients.current_weight` and `clients.current_body_fat_percentage`, because the Overview, the goal strip and the energy calculator want "now" without a query. They are set from the newest row **by `recorded_on`** (a backdated row never regresses the cache — the rule the entries service already has, now the only rule), by one function in one service. *D8 (§3) proposes deleting this rule: both caches go, and "now" is read from the log through a view. If D8 is taken, rule 6's "reads the cache" reads "reads the newest row".*
5. **A check-in owns no measurement columns.** Its readings are rows with `source = 'check_in'` and `source_id = the check-in id`. The `CheckIn` domain object still carries `weight`, `bodyFatPercentage` and the five girths — the read layer assembles them from those rows — so every consumer of the object, including the RN wire, is unchanged. *Under D9's correction path, "its readings" are the latest LIVE rows carrying the check-in's stamp (`source_id`), whatever their source: a correction is a new stamped row on the check-in's day, and the report reads it.*
6. **"Where they stand" reads the cache; "what this check-in reported" reads the stamped rows.** *Refined by commit 8b (owner, 2026-09-03): "where they stand" is read AT A DATE — today on the Overview and the Journey, the check-in's own day on its review, with the goal version and the deadline clock of that day.* Goal position, energy and drift use rule 4. The review band, the AI prompt and the client's check-in detail use rule 5. **The band's "vs last check-in" compares this check-in's stamped rows with the previous check-in's stamped rows, and nothing else**: a measurement logged between two check-ins (source `client_log` or `coach_entry`) never enters that comparison — it belongs to the Journey series and the cache (owner decision 2026-08-31, kept). A check-in whose form carried no weight has no weight row, and the band's cell shows its empty state for that check-in while the strip still shows the client's position from the latest reading. *Under D4 as revised, "since start" reads the derived baseline — the client's reading as of `start_date` — and never a stored start pair.*
7. **Every wire shape stays byte-identical:** `GET /api/client/check-ins`, `GET /api/client/check-ins/[id]`, `GET /api/client/progress`, `GET /api/check-in/[id]`, `GET /api/check-in/[id]/comparison`, the measurement-series route. Proven with recorded responses before and after (§5). One deliberate content change: `GET /api/client/progress` gains coach-logged readings (D6), so its proof compares shape, not values.

**What is deleted at the end (commit 3):** the seven measurement columns on `check_ins`; the `body_metrics` table and every dual-write into it; the physique rows of `client_metric_entries` and its upsert-replace path for physique keys; `utils/metric-points.ts` and its tie-break; the client-side merge in `use-merged-metrics`; the sequential whole-history check-in pager the Journey used only to read two numbers per row.

**What appears for free, not built here:** girths at intake and client-logged girths need only a writer; the table already accepts them. Follow-up, not in scope.

## 3. Decisions for the owner — answer before commit 2

| # | Decision | Recommendation | Why |
|---|---|---|---|
| D1 | Grain and mutability of the log | Append-only; the day's value is the latest row by `recorded_at` (§2 rules 1–2) | One rule replaces two: the immutable history log and the replace-per-day entries table. Corrections keep their history. *Superseded for corrections 2026-09-03 (owner, commit 8): a wrong value is edited IN PLACE, not appended; the day's value is the reading written last — an edit changes a value and nothing else, never a day's order or value (owner revision at execution, 2026-09-03) (D23)* |
| D2 | Wellness keys | Out of scope. `client_metric_entries` keeps its wellness rows; the Journey's Wellness pane keeps its current merge. *Superseded 2026-09-03 (owner): the pane reads the client's daily logs through a route (D16 revised, commits 7 and 9), coach wellness logging is retired (D18 reversed, commit 10) and the client app reads the same kernel (commit 11) — wellness stays on the daily-logs model, which is the point* | Wellness has its own source of truth (daily logs, Phase 6). Mixing it in doubles this workstream. Record as a follow-up in TECHNICAL-DEBT. |
| D3 | `client_metric_entries` after the backfill | Physique rows are deleted in commit 4 once equality is proven; the table stays for wellness (D2) | Two copies of a waist is the bug this plan exists to end. |
| D4 | The start pair (**revised 2026-09-02** after the owner's start-date scenario; the first recommendation, cache-only columns, is now the alternative) | **No start pair is stored anywhere. The baseline is derived: the client's reading as of `start_date`** — the latest live row on or before the start date, else the earliest live row after it — per metric, from one function `getBaseline(clientId)` that the Overview band, the Journey hero, the check-in comparison's start leg and the client app all read. `clients.start_date` stays the one stored origin; `clients.starting_weight / starting_body_fat_percentage` are dropped in commit 3 (the RN wire keeps `startingWeight` / `startingBodyFatPercentage`, derived). The intake or manual-add weight is an ordinary `intake` row dated the day it was captured; activation still refuses without a live weight row; `recordClientStart` writes the date and nothing else. The details sheet's Baseline fields append a `coach_entry` row dated on the start date (withdrawing one is a D9 void), which the as-of rule then reads. **Moving the start date re-derives the baseline and re-dates nothing.** A reading dated before the start date stays in the log, is listed on the Journey under "Before start", and is excluded from the journey's chart and maths. Every surface that shows the baseline shows its date and source (`88 kg · intake, 1 Apr`). **Rendering (owner, 2026-09-02):** the Overview card's big number and the Journey hero's Current are "now" — the newest reading, any date — and never wait for the start date; only the chart line and every "since start" figure do, and while the start date is ahead they read `Starts d MMM` (the Current-plan card's convention). The Overview chart draws the baseline at the start date, as the value in force that day, with the reading's own date in its label. A future start date is allowed | Today the Journey hero's "total change since …" takes the first point of whatever range it loaded (`utils/metric-derived-stats.ts:36-48`) while the Overview's "Since start" subtracts the stored column — two plausible numbers, no error state, visible right now (−12.2 since 8 June beside −14.0 since start). A stored start pair is a number that can exist nowhere in the log; a derived baseline cannot disagree with the chart, because it IS the chart's first point, and cannot be falsified by an edit, because there is nothing to edit. A client logging a pre-start weight (no such writer exists today — the RN `client_log` path will be one) changes nothing silently: it is shown as before-start, and if it is the latest reading on or before the start date it IS the baseline, which is what "what they weighed when we began" means. Alternative (the original recommendation): cache-only columns edited in the sheet, first point synthesised from them, moving the date moves nothing — simpler to build, but the baseline is unverifiable and the Journey/Overview split above survives. Depends on D9 only for withdrawing a wrong baseline reading. |
| D5 | The TDEE provenance event nutrition-plan generation writes to `body_metrics` | Drop it. The plan version row already stores `bmr` / `tdee` | The event has no reader. |
| D6 | The client app's read path for the new table | `client-portal-progress` reads `check_ins` through `createPortalClient()`, i.e. under RLS with the client's JWT. Either add a `clients_view_own_measurements` policy (the shape `body_metrics` has in mig 064) **or** move that read behind service role in a route that has already verified the client. Recommendation: the policy, proven by the harness (§5), since that keeps the portal's data path as it is. The policy is scoped by client, never by source: the client sees every reading about them — check-ins, coach entries, their own logs, intake — and never a removed one (owner, 2026-09-02). `GET /api/client/progress` therefore gains coach-logged readings by design, and its wire proof compares shape, not values | Memory: anon+RLS is load-bearing on the portal path; a table with RLS enabled and no policy is invisible to it. `npm run check:rls` after either choice. |
| D7 | The audit event name `BODY_METRICS_CREATE` | Rename to `MEASUREMENT_CREATE`; no other audit change | Name follows the table. |
| D8 | The two "current" caches (raised by the owner 2026-09-02 after a hand-deleted check-in left `clients.current_weight` at a value no reading carried) | **Drop both.** Nothing reads a card; every "where are they now" reader takes the newest row of any source from the log, through one view `client_current_measurements` (one row per client: newest weight and newest body fat by `recorded_on DESC, recorded_at DESC`, `WITH (security_invoker = on)`). `getClientById` / `mapClientRow` read the two numbers through it, so `Client.currentWeight` / `currentBodyFatPercentage` keep their names and every consumer of the object is unchanged. Rule 4 is deleted; `appendMeasurements` writes no cache and instead calls `recalculateClientEnergy` when the appended row is the client's newest (the same predicate the cache guard uses today). Commit 3 drops the two columns. **Depends on D6 = the policy**: the client app reads under its own JWT, and the view only shows a client their rows if that policy exists. Alternative: keep rule 4, with the setter as `refreshMeasurementCache(clientId)` — a recompute from the log, runnable on its own — so a manual delete is healed by re-running it | A cache is a copying rule, and every copying rule here has produced a reader that disagrees with another (§1); the hand-delete showed the Overview contradicting itself on one card (chart 76.0, chip "4.0 kg to go", footer −14.0). With the view there is nothing to go stale and no cleanup procedure. Two readers already prefer the newest reading over the cache today — `nutrition-calc-inputs.ts:124` (`latestMetrics?.weight ?? client.currentWeight`) and the client app's `goals-section.tsx:30` (`client.currentWeight \|\| latestWeight`) — so D8 makes every reader behave as those two do. |
| D9 | Retracting a wrong reading (raised by the owner 2026-09-02) | **A void mark, not a delete.** `voided_at timestamptz`, `voided_by uuid`, `void_reason text` on `client_measurements`; one RPC `void_measurement(p_id, p_client_id, p_actor, p_reason DEFAULT NULL)` (SECURITY DEFINER, `GRANT EXECUTE … TO service_role`, refuses an already-voided row and a row outside `p_client_id`) is the only UPDATE the table ever sees — the app role's grant stays `SELECT, INSERT`. A view `client_measurements_live` (`security_invoker`, `WHERE voided_at IS NULL`) is what every reader reads, the `client_current_measurements` view of D8 included, so a removed row leaves every calculation and every client surface at once — Journey series, Overview, the strip, the client's check-in detail and progress series, the AI prompt — and the history keeps it; the coach's measurement list still shows it, muted, with who removed it and when. **Restore** (owner, 2026-09-02): a removed row is brought back with one click, through the same RPC family (`restore_measurement` clears the three columns; refuses a live row), audited as `measurement.restore` — CONVENTIONS §8's soft-delete rule asks for a way to reactivate, and re-typing a value the coach can see on the muted row is the clumsy alternative. **A wrong value is corrected, not removed** (owner, 2026-09-02): `correctMeasurement` appends a new row with the original's metric, day and stamp (`source_id`) and the corrector's own source, so a check-in's report, the Journey and every "now" surface read the corrected value while the wrong one stays in history; a void is for a reading that should never have existed. Edit reading (live rows), Remove reading (live rows) and Restore reading (removed rows) are the three row actions on the Journey's measurement log. **Coach only for now**, from the Journey's measurement log ("Remove reading" behind the destructive-confirm dialog), audited as `measurement.void`; a client's own `client_log` rows can reach the same RPC behind a client route later, additive to the RN contract. Voiding the newest reading recomputes energy — the same trigger as appending one. Built as **commit 4, after the old stores are gone**, so the equality proofs of commits 2–3 never meet a voided row and the check-in columns need no mirror update | HealthKit lets a user delete a sample; FHIR marks an observation "entered in error"; CONVENTIONS §8 says user-created data is soft-deleted, never hard-deleted. Without it, a wrong reading on a day with no other reading can only be fixed with database access. Alternatives: a "void row" (INSERT-only purity — every read anti-joins, rule 2 becomes a subquery, harder to index); a hard DELETE (rejected: the history is the point); re-typing the value from a muted row instead of a restore (rejected: clumsy, and the conventions ask for reactivation). *The CORRECTION path is superseded 2026-09-03 (owner, commit 8): Edit changes the row in place and keeps its stamp; Remove and Restore stand as built* |
| D10 | When a reading was taken (raised 2026-09-02, ahead of two planned features: wearables and per-meal nutrition) | **Add `measured_at timestamptz` to the table now, nullable.** The moment the reading was taken, distinct from `recorded_at` (when it was written). The client app and devices set it; a coach entry or an intake reading that knows only the day leaves it null. Rule 2 stays "latest `recorded_at` wins" until devices arrive; it then becomes "latest `measured_at` wins, null first", with no re-dating. The check-in writer sets it to the submission time; the backfill leaves it null | A morning and an evening weigh-in are different readings, and a wearable that syncs yesterday's data this afternoon writes it later than a coach's correction made this morning — under "latest written wins" the late sync would silently override the correction. One nullable column on a table that does not exist yet costs nothing; adding it later is a migration and a backfill of nulls. The only schema change the two planned features need now: wearables later widen the `metric_key` CHECK, meals later get their own table. |
| D11 | "Did the client log today?" (raised 2026-09-02; `lib/engagement-triggers.ts:26-32` records that training and habit logging never create a `daily_logs` spine row, so the no-engagement alert unions three tables itself while the check-in header's "N/M days logged" and the logging-gap alert count spine rows and miss a training-only or habits-only day) | **Derive it, once — commit 5.** A logged day is a day with any log the client made themselves: a wellness row, a nutrition row, a habit log, a workout log, or — once the client can log measurements — a `client_log` row in the notebook. One pure function, `loggedDays` in `lib/logged-days.ts`, computes the set from those five sources and every reader asks it. **Coach entries and check-in submissions do not count** (owner, 2026-09-02): the question is daily engagement, not the coach's work or the weekly report. No writer touched, no migration. A stored flag on the spine row is not the answer: it is a copy five writers must remember to maintain, and two already forgot — that is today's split | The spine row is the parent of the client's day-form (wellness, nutrition, the day note), not an activity flag. The engagement alert's private union is the correct definition living in the wrong place; the fix is to make it the shared one. Same shape as goal position and the baseline: one kernel, one scan test. |

**D8 reader map** (grep 2026-09-02: 43 non-test files touch the two columns; no SQL function or RPC reads them). Every reader keeps its field and its logic; only where the number is looked up from changes. What must be edited by hand: four **string-built column lists** that name the columns — `services/client-energy-service.ts:96`, `services/client-portal-service.ts:53` (`/api/client/me`, the RN contract; its `if (error \|\| !data) return null` swallows a stale name into an empty profile), `services/client-portal-progress.ts:140` (`/api/client/progress`, rule 7) and `services/intake-review-service.ts:110` — a stale column name there is a PostgREST 400 that `tsc` cannot see.

- **Energy** (`services/client-energy-service.ts:157-160`): `recalculateClientEnergy` feeds `weightKg` / `bodyFatPercentage` into the pure calculator from its own select of `clients`; that select reads the view instead. Formula, pair-atomic UPDATE, override rules, and who triggers a recompute — unchanged; the trigger moves from "cache updated" to "newest row appended", the same event.
- **Nutrition builder** (`services/nutrition-calc-inputs.ts:124-152`): already `latestMetrics?.weight ?? client.currentWeight`; the first leg becomes the log's newest row (commit 3), the second goes with the column. `nutrition-service.ts` (pure, takes `currentWeightKg`) and the orchestrator's `baseWeightKg` snapshot — unchanged.
- **Drift notes**, two of them: `lib/goals/detect-goal-drift.ts` compares GOALS (its `currentWeight` is the goal weight) — untouched; the 3 kg weight-drift banner is the pure `shouldShowRegenerationBanner(currentWeightKg, baseWeightKg)` — untouched; both callers (`comparison-service`, the nutrition hero) pass `client.currentWeight` off the object.
- **Intake sync** (`services/intake-review-service.ts:125-144, 200-206`): "fill only when the client has no reading" — the guard reads the view instead of the column; the write becomes an `intake` row plus the start pair per D4. `client-intake-service.ts:17` and `app/api/client/intake/route.ts:11` read `client_intake`'s OWN `current_weight` column — untouched.
- **Client app** (`services/client-portal-service.ts:53` → `/api/client/me`; `services/client-portal-progress.ts:140,263` → `/api/client/progress`; `components/client-portal/metrics/goals-section.tsx:30`): the wire keeps `currentWeight` / `currentBodyFatPercentage` (additive-only contract), sourced from the view under the client's JWT — hence the D6 dependency.
- **Also on the list, all read the field off the `Client` object and change nothing:** `lib/client-profile-completeness.ts:44-111` (`hasStartWeight` — the activation gate — and `findProfileGaps`), `components/clients/overview/status-band.tsx`, `check-in-goal-strip.tsx` via `comparison-service.ts`, `details-groups.tsx`, `lib/blocks/block-derivations.ts` (pure, fed by the Journey series). **Writers** that set the columns today (`client-service.ts` create/update, the metrics route, `metric-entries-service.ts`, `body-metrics-service.ts:87-89`, the seeds, `lib/validations/client.ts`'s PATCH fields) are commit 2's writer switch; the PATCH field names on the wire stay.

**Owner's answers** (all given 2026-09-02): D1 — yes, append-only, the day's value is the latest live row · D2 — yes, the seven body measurements only; wellness stays on the daily logs · D3 — yes, the physique entry rows go in commit 3 · D4 (revised) — yes, no stored start pair, the baseline is the reading as of the start date; the card's number is "now", only the chart line and "since start" wait for the start date · D5 — yes, the TDEE event stops · D6 — the policy, scoped by client; the client sees every reading about them; the progress route gains coach readings by design · D7 — yes · D8 — drop both current columns; "now" is the newest reading through the view · D9 — correct, remove and restore; coach only for now · D10 — add `measured_at` · D11 — derived once; coach entries and check-in submissions do not count

## 4. Blast radius — every dependant, by subsystem

Verified by grep on 2026-09-02. **Training has no dependency on body measurements** (every "weight" in `services/training*` is a lifted load); neither do the attention feed, tracking, or the check-in period snapshot.

| Subsystem | Today | After | Commit |
|---|---|---|---|
| Check-in submit — `app/api/client/check-ins/route.ts`, `services/client-check-in-service.ts`, `utils/check-in-canonical-metrics.ts` | writes the check-in columns, the cache and a `body_metrics` event | appends stamped rows; the columns, the cache and the event are no longer written | 2, 3 |
| Check-in object assembly — `services/check-in-service.ts` (`getCheckInById`, `getClientCheckIns`, `getPreviousCheckIn`, `getFirstCheckIn`), `lib/mappers.ts` | `select("*")` on `check_ins` | one extra query per call: rows with `source_id IN (…)`, folded into the object; the object is unchanged | 2 |
| Review page — `components/check-in/kpi-ribbon.tsx`, `utils/ai-prompt-builder.ts`, `services/comparison-service.ts` | reads the check-in object; the comparison also reads `body_metrics` for a starting-value fallback and ten check-in rows for the trend | object readers untouched; goal position from the cache (commit 1); the fallback and the trend read the log (commit 2) | 1, 2 |
| Goal strip — `components/clients/check-ins/check-in-goal-strip.tsx`, `types/check-in.ts` (`GoalProgress`) | a row only when the check-in has the reading | a row per set goal; `position` null when the client has no reading at all | 1 |
| Client app — `app/api/client/check-ins/*`, `app/api/client/progress/route.ts`, `services/client-portal-progress.ts`, `app/client/check-in/[id]/page.tsx`, `components/client-portal/check-in/check-in-card.tsx`, `components/client-portal/metrics/metrics-hub.tsx` | the wire is assembled from `check_ins` columns | wire identical; the progress series is built from the log (D6) | 2 |
| Coach Journey — `components/clients/metrics/hooks/use-merged-metrics.ts`, `use-metrics-data.ts`, `utils/metric-points.ts`, `utils/metric-derived-stats.ts`, `hooks/use-metric-entries.ts`, `hooks/use-check-in-data.ts` (`useAllClientCheckIns`) | pages the whole check-in history sequentially, merges with entries client-side, tie-break | one series request per client from the log (extend the measurement-series route to all seven metrics with notes and sources); the merge and the pager go | 2, 3 |
| Overview progression chart — `services/measurement-series-service.ts`, `app/api/clients/[id]/measurement-series/route.ts` | server-side copy of the merge | reads the log; becomes the Journey's series too | 2 |
| Overview status band, goal chips — `components/clients/overview/status-band.tsx`, `lib/goals/goal-state.ts` | the caches, through the client object | the component is unchanged; its client object is filled from `client_current_measurements` and `getBaseline` (D8, D4 revised) | 2 |
| Coach entries — `app/api/clients/[id]/metric-entries/route.ts`, `services/metric-entries-service.ts`, `types/metric-entries.ts` | upsert-replace + dual-write + conditional cache | physique keys append to the log (rules 2–3); wellness keys unchanged (D2) | 2, 3 |
| Coach metrics route — `app/api/clients/[id]/metrics/route.ts`, `services/client-service.ts` (`metrics_api`) | cache + `body_metrics` event | appends `client_log` / `coach_entry` rows | 2 |
| Intake — `services/intake-review-service.ts`, `services/client-intake-service.ts`, `app/api/client/intake/route.ts` | cache + start caches + `body_metrics` event (`intake_sync`) | appends an `intake` row for weight and body fat; no caches (D8, D4 revised) | 2 |
| Start pair — `services/client-start-service.ts`, `components/clients/overview/use-client-profile-edit.ts`, `services/client-service.ts` | entries rows on the start date + caches + dual-write | D4 revised: the date only; the Baseline fields append a `coach_entry` dated on the start date | 2, 3 |
| Nutrition builder / cascade — `services/nutrition-calc-inputs.ts`, `services/nutrition-plan-service.ts`, `services/nutrition-plan-orchestrator.ts`, `services/nutrition-service.ts`, `utils/nutrition-helpers.ts`, `lib/goals/detect-goal-drift.ts` | plans against the cache, `body_metrics` as a rescue when the pair is null; snapshots the base weight on the plan; the drift note compares cache to base; writes a TDEE event | behaviour unchanged; the rescue reads the log; D5 | 2 |
| Energy — `services/client-energy-service.ts`, `services/client-energy-calc.ts` | reads the cache columns; stamps bmr/tdee onto events | reads the view (D8); recomputed when a newest reading is appended or voided; stamping goes with `body_metrics` | 2, 3 |
| Activity feed — `services/client-activity-feed-service.ts` | lists entries rows | lists log rows with `source = 'coach_entry'` | 2 |
| Journey blocks — `services/client-journey-service.ts`, `lib/blocks/*`, `components/clients/metrics/blocks/blocks-subtab.tsx` | weight points from the merge | from the log series | 2 |
| Seeds and scripts — `scripts/seed/generate.ts`, `scripts/seed-scale-client.ts`, `scripts/perf-baseline.ts`, `scripts/seed/teardown.ts` | write check-in weights, `body_metrics`, entries | write the log | 2, 3 |
| Security — mig 064 policies on `body_metrics`, `lib/constants.ts` audit names, `npm run check:rls` | two tables' policies | one table: RLS enabled, `service_role` grant, D6 policy; D7 | 2, 3 |
| Docs — ARCHITECTURE "body_metrics table", "client_metric_entries table", "The client's origin", the review-surface payload paragraph, Journey tab row; CONVENTIONS §8 examples; TECHNICAL-DEBT ledger | describe three stores | describe one | 3 |
| Both databases — DEV `aeaphsslctwcmebldrzx`, PROD `etezzztgafcotyahgijk` | at 157 | one migration each for commits 2, 3, 4, 8 and 10, numbered with **the next free numbers at execution time**: `docs/TRAINING-COMPLETION-EXECUTION-PLAN.md` also names 158 and 159, and whichever workstream pushes first takes them (never reuse, never skip); PROD after `migration list --linked` | 2, 3, 4, 8, 10 |
| Logged-day readers — `lib/engagement-triggers.ts`, `lib/tracking-triggers.ts`, `services/attention-feed-service.ts`, the check-in detail payload and header (`components/clients/check-ins/check-in-detail-view.tsx:115`) | three definitions of a logged day | one derived definition (D11), no migration | 5 |
| Coach wellness logging — the Log-measurement dialog's five wellness keys, `client_metric_entries`, the entries GET and POST, the activity feed's entry rows, the Journey's browser-side merge | a coach can log a client's mood, energy, sleep, stress and soreness; the Wellness pane merges the entries with the check-ins' averages | retired — a wellness score is the client's self-report (owner, 2026-09-03): the pane reads the client's daily log (7 and 9), the dialog loses the keys (9), the table and its readers go (10), the client app reads the same kernel (11) | 7, 9, 10, 11 |
| Editing a reading — `services/measurement-edits-service.ts`, `appendCorrection`, `POST …/measurements/[id]/correct`, the day-value kernel, the two "now"/baseline views, the Journey's log rows and section, `scripts/measurement-edit-proof.ts` | a correction is a NEW stamped row; the log folds a day's readings under its lead | edited IN PLACE (owner, 2026-09-03): one update RPC, `updated_at`, the day's value = the reading written last — an edit changes a value and nothing else, never a day's order or value (owner revision at execution, 2026-09-03), the log one row per reading with Edit on each, the fold deleted | 8 |
| The check-in review's goal strip — `services/comparison-service.ts`, `check-in-goal-strip.tsx`, `lib/goals/goal-progress-ownership.test.ts`, `getClientCheckIns`' trend read, the drift note | reads NOW: the client's newest reading, the live goal, today's clock, the client's latest ten check-ins, the nutrition version covering today | reads the check-in's DAY (owner, 2026-09-03): the check-in's own reading or the one before it, the goal version in force then, days remaining from then, the ten check-ins up to it, the nutrition version covering it | 8b |
| The nutrition deficit's window — `hooks/use-nutrition-builder.ts`, `nutrition-settings-form.tsx`, `drawer-footer.tsx` and `ApplyDateDialog`, `services/nutrition-plan-orchestrator.ts`, `services/nutrition-service.ts` | the effective date is asked after Generate and reaches only the version's window; the preview and the save spread the deficit from today, or from the goal's start date when that is in the future | the date is a drawer setting picked before the save; the preview and the save compute from it (owner, 2026-09-03) | 8bb |
| The goal's start date — `client_goals.goal_start_date`, both zod schemas and their refine, the sheet's Goal start field and the deadline floor composed from it, `resolveEffectiveGoal`'s `startDate`, `nutrition-calc-inputs.ts` | a third "start" beside the client's and the blocks', ignored when past, compressing the deficit into today when future, typed twice to agree with the plan | dropped; the plan's effective date is the window's start (owner, 2026-09-03) | 8bb |
| Closing a reported period — `lib/daily-log-permissions.ts`, `services/daily-log-permissions-service.ts`, the four client write routes, `services/training-event-layout-service.ts`, the five client-side readers, `locked-day-notice.tsx`, `/api/client/me` | today editable, a past day locked only once it is logged, a never-logged day open for ever; the client calendar move carries its own past-day rule | a day is locked once it falls on or before the last submitted check-in's `period_end` (owner, 2026-09-03); the logged-day lock and the move's backfill read are deleted; `closedThrough` on the wire | 8c |
| The nutrition events' forward horizon — `calculateNutritionEndDate`, `resolveScopeDates`, the `{kind:"from"}` cascades | a fixed 8 weeks from the anchor with no top-up and no scheduler, so the wall reaches today mid-block and the client's day screen, their food log's stored target and the coach's data table all read blank | the later of the anchor + 8 weeks and the furthest live program end — queued programs included, since the read carries no date predicate — resolved per generation (owner, 2026-09-04); the floor means nothing that works today can generate less | 8cc |
| The goal's second store — `clients.goal_weight` / `goal_body_fat_percentage` / `goal_deadline`, the mirror UPDATE in `updateGoals`, the mirror legs of `toClientGoalInput`, the two client wires and the intake sync reading the copy, `primary_goal` | a goal written to two stores in three autocommitted round trips; every save mints a version | one store, one RPC that merges and writes nothing on no change; the copy dropped; `Client` and the wires filled from `client_current_goals` (owner, 2026-09-03) | 8d |
| The goal's liveness — `client_goals.effective_from` (an instant stamped at the write) and `superseded_at`, `getCurrentGoals`, `getGoalAsOf`, `updateGoals`' supersede-and-insert, the sheet's Goals & energy group, the drawer's `calcInputs` taken once at load | a goal goes live the moment it is saved; a goal set ahead of a block shows on every surface at once and is judged against check-ins before its day; the drawer prices a queued plan against today's goal | date-ranged versions on the client's calendar, close-and-insert with the absorb rule, one open row, no overlaps, no backdating, replace only; every reader resolves by date — the client's today, the check-in's day, the plan's effective date (owner, 2026-09-04) | 8d |
| The block's target — `client_phases.target_weight`, the block form's field, `block-chain-payload.ts`, the subtab's pace readout and `derivePace`, the journey wire's `targetWeightKg` | a second target concept beside the goal, fed to no calculator: a pace toward a number no plan was built for | dropped; a block reads the goal version covering its reference date, and the wire field derives (owner, 2026-09-04) | 8d |
| The day spine — `daily_logs`, its `daily_log_id` FKs, `daily_logs_full`, the per-card ensure-spine writes, the day note | one parent row per day for wellness and nutrition | a plan to flatten it, not a build (commit 12) | 12 |

## 5. Verification that nothing is missed

- **Wire proofs (commit 2).** Record the JSON of the six routes in §2 rule 7 for the fixture client and for Sam Kalepa before the switch, using the request-level harness (mint a session with `generateLink` → `verifyOtp` → cookie, drive `next dev`); diff after. A vitest that mocks `supabaseAdmin` proves nothing here. The RN app is the reason: the client routes are its contract and their shapes must not move.
- **The invariant scans (kept).** Commit 1's `lib/goals/goal-progress-ownership.test.ts` — goal position is built from a client and a goal, never a check-in; commit 2's baseline scan — `getBaseline` is the only derivation of "the reading as of the start date"; commit 5's `lib/logged-days-ownership.test.ts` — a logged day is derived once. Each in the shape of `lib/check-in/adherence-ownership.test.ts`, each mutation-tested.
- **Gates after every commit:** `npx tsc --noEmit`, `npx eslint .`, `npx vitest run`, `npm run check:labels`, `npx knip` (exits clean since `e9e54916`), `npm run check:service-key`, and `npm run check:rls` for commits 2, 3 and 4. Mutation-test every new assertion from a copy in the scratchpad; never `git stash` or `git checkout --`.
- **Migrations:** the next free number at execution time (§4); `supabase db push --dry-run` immediately before every push; the push may be classifier-blocked, hand it over via `!`. Drops as `IF EXISTS`. Regenerate types after each push and diff them against the repo. DEV first; PROD after `migration list --linked`.

## 6. The commits

Each prompt is complete on its own. Paste it into a fresh session. The session must plan for review before writing anything.

### Commit 1 — `fix(goals): goal position reads the client's current reading — one kernel, one source`

**STATUS: SHIPPED `55ed1242`, 2026-09-02.** Owner decision on the footer: a `No reading yet` row neither earns nor blocks "Goal met"; with nothing judged there is no note. A fourth doc edit landed with the owner's OK — ARCHITECTURE's strip paragraph names the `No reading yet` state. The DEV premise had moved by execution time: the record read 74.0 (three check-ins at 74 submitted 15:12–15:15 BST and deleted since), and no weightless check-in survived, so the browser smoke started with a fresh weightless submission. Browser smoke PASSED 2026-09-02, all five items.

Fixes the page on main today. Storage untouched. Also carries the reply-block button fix agreed in the R6 smoke.

- `lib/goals/goal-progress.ts` (new, pure): `deriveGoalProgress` takes `{ effectiveGoal, client: { currentWeight, currentBodyFatPercentage, startingWeight, startingBodyFatPercentage }, trend: { avgWeeklyWeightChange, avgBodyFatChange }, daysRemaining, weeksRemaining }` and returns `GoalProgress`. It composes `calculateGoalProgress`, `deriveGoalStatus` and `computeGoalPace`; the assembly currently inline in `services/comparison-service.ts` (the two goal blocks) moves here. A check-in is not among its inputs.
- `types/check-in.ts` → `GoalProgress`: a `weight` / `bodyFat` entry exists whenever that goal is set; each holds `goal`, the start value, and `position: { current, remaining, percentComplete, status, isOnTrack, paceStatus? } | null` — null only when the client record has no reading for that metric.
- `services/comparison-service.ts`: calls the kernel with the client record's readings; the starting-value fallback chain loses its "current check-in" last resort in favour of the client's current reading; `changes` (the band's deltas) are untouched.
- `components/clients/check-ins/check-in-goal-strip.tsx`: rows from goals; `position === null` renders the goal, the track empty and `No reading yet` in the state column (neutral tone); the empty state renders only when no goal is set; the footer's "all met" reads only rows with a position.
- `components/clients/check-ins/check-in-reply-block.tsx`: the footer row gets `justify-end`, Copy before Send so the primary sits at the far right (divider grammar: actions right).
- Tests: `lib/goals/goal-progress.test.ts` (goal + client reading → full row; goal + no reading → `position: null`; no goal → no entry); `services/comparison-service.test.ts` — the existing expectation that `calculateGoalProgress` receives the check-in's weight (178) becomes the client's current weight (180), plus a case where the check-in carries no weight and the row still exists; strip tests for `No reading yet` and for the empty state only without goals; the §5 invariant scan. Mutation: point the kernel's `current` at the check-in's weight and confirm the "check-in without weight" test fails.
- Docs: ARCHITECTURE → "Goal progress and pace" states rule 6 of §2 (position from the client's current reading; a check-in is a report, never a position source); ARCHITECTURE → "client_metric_entries table" loses the sentence claiming display-unit storage (canonical since mig 141); TECHNICAL-DEBT → "The two goal targets contradict each other" gets the next step (the Overview chips adopting the kernel).

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md before
planning. Plan for my review before writing anything.

This workstream changes the shape of body-measurement storage (plan §2). Commit 1 does
not touch storage, but its rule — goal position comes from the client record's current
reading, never from a check-in row — is new. Where ARCHITECTURE or CONVENTIONS state
something that contradicts §2 or this commit, do not silently follow it and do not
silently override it: list each contradiction with the doc line and what the plan says
instead, and I will review before you write.

Job: Commit 1 of docs/MEASUREMENT-LOG-PLAN.md §6 — `fix(goals): goal position reads the
client's current reading — one kernel, one source`. Build exactly what that section
lists: the pure kernel in lib/goals/goal-progress.ts that takes a client and a goal and
never a check-in; the GoalProgress wire with a row per set goal and `position: null`
when the client has no reading; comparison-service calling the kernel with the client
record's readings; the strip rendering `No reading yet` and showing its empty state only
with no goals; the reply block's footer actions on the right, Copy before Send; the
tests, the invariant scan and the mutation test named there; the three doc edits.

Verify the premise before you plan: on DEV, client f87bee53-0974-46d3-b1fb-34c14af6a8b5
has a live weight goal of 70 kg, a current weight of 75.0 on the client record, and its
newest check-in carries weight null. The review page shows "No goals" for it today.

Rules: current shape only in ARCHITECTURE (no "used to"); every fixture number distinct;
back files up with cp to the scratchpad before mutating, never git stash or git checkout
--; gates after the commit: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip (exits clean at HEAD), npm run check:service-key. Commit directly
to main. Then replace this commit's STATUS line in docs/MEASUREMENT-LOG-PLAN.md §6 with
SHIPPED, the hash and the date, and hand me a browser smokelist for the strip: a client
with a goal and a weightless newest check-in, a client with no goals, a client with one
goal, a client with no reading at all, and the reply block's buttons.
```

### Commit 2 — `feat(measurements): one log for every body measurement — table, writers, readers`

**STATUS: SHIPPED `29456d71`, 2026-09-02.** Deviations, each put to the owner before writing: the backfill is the MINIMAL one — check-in columns, the entries' physique rows and `intake_sync` events only; `body_metrics` `check_in`, `coach_entry`, `metrics_api` and `nutrition_plan` events skipped (DEV counts in the commit body). Three views, not two: `client_baseline_measurements` derives the baseline in SQL so `getClientById` stays one round trip (the view-over-view embed was probed on DEV over REST). The series route is shape-compared, not byte-compared: the top-level metric keys stay, five siblings join them, each point carries `source`/`note`/`id`/`recordedAt`, and `baseline` + `startDate` are added; `from` is gone. Rule 3 keys on the stamp as well as the source. `created_by` stores `coaches.id`. The nutrition rescue's energy leg is gone (the log stores no pair). Interim until commit 4: clearing a body fat is refused with a readable 400, and a hand-deleted check-in leaves its stamped rows live. The Baseline field before activation appends an `intake` row dated today. The entries service's second inches→cm conversion — a live bug: a metric coach's 68 cm waist was stored as 172.72 — is gone by construction; the two stored rows were carried as they are. knip pulled commit 3's deletions of `body-metrics-service`, its types, `getFirstCheckIn` and `updateClientMetricsFromCheckIn` into this commit; `utils/metric-points.ts` and `useAllClientCheckIns` SURVIVE for the Wellness pane (D2), so commit 3's "delete the merge" bullet must read "delete the physique half". Wire proofs: list, client detail and coach detail byte-identical for both clients; comparison and `/api/client/me` (added to the set) shape-kept — the fixture's cache said 170 kg against readings near 80, the stale cache D8 exists to end; progress and series shape-kept by design. Migration 158 taken; the training-completion plan's numbers move to 159/160. Browser smoke PASSED 2026-09-02 (owner: all green).

- Migration (the next free number): the table in §2, `measured_at` included (D10); RLS enabled; `GRANT SELECT, INSERT ON public.client_measurements TO service_role` and nothing else (rule 1); the D6 policy; two views, both `WITH (security_invoker = on)`: `client_measurements_live` (a plain `SELECT *` until commit 4 adds the void filter, so every reader is on the right name from the start) and `client_current_measurements` (D8: the newest live weight and body fat per client). The backfill in the same migration, one plain `INSERT … SELECT` per old store: `check_ins` (seven columns → rows, `source = 'check_in'`, `source_id = check_ins.id`, `recorded_on = created_at::date` in the client's timezone — the Journey's existing convention — `recorded_at = created_at`, `measured_at = created_at`); `body_metrics` (weight and body fat rows for sources `metrics_api` → `client_log`, `intake_sync` → `intake`, `coach_entry` → `coach_entry`; **skip** `check_in` events, already covered by the stamped rows, and `nutrition_plan` events per D5; `recorded_on = recorded_at::date`); `client_metric_entries` physique keys (`source = 'coach_entry'`, `recorded_on = entry_date`, `recorded_at = updated_at`, `note`, `created_by`). No start-pair rows (D4 revised). `gen types` after the push; diff.
- `services/measurements-service.ts` (new): `appendMeasurements({ clientId, source, sourceId?, recordedOn, measuredAt?, values: Partial<Record<MetricKey, number>>, note?, createdBy? })` implementing rules 2–3 in one place (skip-on-unchanged) and calling `recalculateClientEnergy` when the appended row is the client's newest weight or body fat (D8 — the same event the cache update used to be); `getMeasurementSeries(clientId, { metricKeys, from?, to? })` returning day-values by rule 2 with source and note; `getMeasurementsForCheckIns(checkInIds)` — the latest live row per (`source_id`, metric); `getBaseline(clientId)` (D4 revised: the reading as of `start_date` per metric, with its date and source), guarded by a scan test that nothing else derives it. Canonical units in, canonical units out (CONVENTIONS §20).
- Writers move to the log and stop writing the old stores: check-in submit (`services/client-check-in-service.ts` → stamped rows with `measured_at = now()`; the check-in columns, the cache and the `body_metrics` event are no longer written); coach entries (`services/metric-entries-service.ts`: physique keys append, wellness keys unchanged — D2); the coach metrics route and `client-service`'s `metrics_api` path; intake (an `intake` row when the weight is captured); the start pair per D4 revised (`recordClientStart` writes the date only; the details sheet's Baseline fields append a `coach_entry` row dated on the start date; the entries-row seeding and its dual-write go — they were the phantom-duplicate path); nutrition plan generation per D5 (no TDEE event). The four string-built column lists in the D8 reader map are edited here. Seeds (`scripts/seed/generate.ts`, `scripts/seed-scale-client.ts`, `scripts/perf-baseline.ts`) write the log; `scripts/seed/teardown.ts` relies on the `ON DELETE CASCADE` from `clients` rather than deleting log rows — the app role has no DELETE (rule 1), and that is deliberate.
- Readers move to the log, the wire proofs (§5) recorded first for the fixture client `5ca1ec1e-0000-4000-8000-000000000001` and `f87bee53-0974-46d3-b1fb-34c14af6a8b5`: the check-in object assembly (`services/check-in-service.ts`'s four readers fold `getMeasurementsForCheckIns` in; `lib/mappers.ts` stops mapping the columns; the object and the RN wire unchanged); `getClientById` / `mapClientRow` fill `currentWeight` / `currentBodyFatPercentage` from `client_current_measurements` and `startingWeight` / `startingBodyFatPercentage` from `getBaseline` (D8, D4 revised — every consumer of the `Client` object, `/api/client/me` included, is unchanged); the Journey (the measurement-series route and service serve all seven metrics with day-values, sources and notes; `use-merged-metrics` consumes it; the merge, the tie-break and `useAllClientCheckIns`' Journey role go — the hook may stay if the check-in history list still uses it, grep at execution time; the measurement log lists readings dated before the start date under "Before start"); the Overview chart and the blocks read the same series; `services/client-portal-progress.ts` builds its series from the log (D6); `services/comparison-service.ts`' start leg and ten-row trend read the log; `services/nutrition-calc-inputs.ts`' rescue reads the log; the activity feed lists `coach_entry` rows; every baseline reader — the Overview band's "Since start", the Journey hero's "total change since …" (`utils/metric-derived-stats.ts`, which today takes the first loaded point), `/api/client/me`, `/api/client/progress` — reads `getBaseline`.
- Rendering (D4 revised): the Overview band's big number and the Journey hero's Current are the newest reading, any date; the chart line and every "since start" figure read `Starts d MMM` while the start date is ahead; the Overview chart draws the baseline at the start date with the reading's own date in its label.
- Wire proofs diffed at the end; a §2-rule-7 note in the commit body per route. Every wire must be byte-identical; a difference is a defect to fix, never a fixture to update — except `GET /api/client/progress`, whose values gain coach-logged readings by design (D6) and whose proof compares shape.
- Tests, each with a mutation: the service (rules 2 and 3, the energy trigger, the baseline's three cases — a reading on the start date, only readings before it, only readings after it), every switched writer and reader, the baseline scan; `npm run check:rls`.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md before
planning. Plan for my review before writing anything.

This workstream changes the shape of body-measurement storage: one append-only table,
client_measurements, replaces the check-in's measurement columns, the body_metrics event
log, the physique rows of client_metric_entries and the four weight columns on clients
(plan §2, D4 revised, D8). ARCHITECTURE describes the current shape in "body_metrics
table", "client_metric_entries table", "The client's origin", "Client energy" and
"Dual-write pattern", and CONVENTIONS §8 cites those tables as examples. Where either
doc states a rule that contradicts §2 or this commit, do not silently follow it and do
not silently override it: list each contradiction with the doc line and what the plan
says instead, and I will review before you write. Read my answers to D1–D8 in §3; if any
is still blank, stop and ask.

Job: Commit 2 of docs/MEASUREMENT-LOG-PLAN.md §6 — `feat(measurements): one log for every
body measurement — table, writers, readers`. Build exactly what that section lists, in
this order: record the wire proofs for the six routes in §2 rule 7 with the request-level
harness (a real session via generateLink → verifyOtp, a running next dev, scripts inside
the project tree); the migration with the table, RLS, the INSERT/SELECT-only grant, the
D6 policy, the two views and the backfill; the measurements service; every writer in the
blast-radius table (§4) moved to the log and off its old store; every reader in that
table moved to the log; the seeds; the tests and mutations; check:rls; diff the wire
proofs at the end. Grep at execution time for every reader and writer in §4; do not
trust the table.

Migration rules: the next free number (§4 — another plan also names 158 and 159); drops
as IF EXISTS; `supabase db push --dry-run` immediately before the push; the push may be
classifier-blocked — hand it to me via `!` if so; DEV only (linked ref
aeaphsslctwcmebldrzx); regenerate types after the push and diff them; never mark a
migration applied by hand. Rules: the RN app is the real client — its routes are the
contract; every fixture number distinct; cp backups to the scratchpad before mutating,
never git stash or git checkout --; gates: npx tsc --noEmit, npx eslint ., npx vitest
run, npm run check:labels, npx knip, npm run check:service-key, npm run check:rls.
Commit directly to main. Then replace this commit's STATUS line in §6 with SHIPPED, the
hash and the date, and hand me a browser smokelist covering the Journey Physique pane
and its measurement log, the Overview status band and chart, the check-in review page
for a weightless check-in, the client app's progress screen and a check-in detail with
girths.
```

### Commit 3 — `chore(measurements): the old stores go — columns, table, merge; docs`

**STATUS: SHIPPED `24a2f667`, 2026-09-03.** Migration 159 on DEV and PROD (PROD took 158 and 159 in one push; its regenerated types are identical to the repo's; `check:rls` green on both, 46 tables). Deviations, each put to the owner before writing: no equality guard inside the migration (owner, 2026-09-03; the DEV equality counts are in the commit body); the merge layer's physique half was the body definitions' check-in-field binding, deleted by splitting `MetricDefinition` into body and wellness types so `buildMetricPoints` accepts wellness definitions only — `utils/metric-points.ts` and `useAllClientCheckIns` survive for Wellness (D2); the entries service had no physique path left (physique keys already append to the log), so the narrowed CHECK is the belt; the energy service's stamping was already gone, its comments rewritten. Four doc edits beyond the list, each with the owner's OK: CONVENTIONS §8's new-table grants paragraph now states the probed default privileges (a REVOKE, then exactly the grants the table needs); ARCHITECTURE's stale `use-merged-metrics` clause under "Effective goal resolution"; the "Client Metrics Log Extraction" ledger row deleted rather than marked resolved; `docs/perf-baseline.md`'s progress-read followup narrowed to the goal mirror. 160 is the next free migration number, for commit 4 and the training-completion plan alike. Browser smoke PASSED 2026-09-03 on all four items, with three findings: the blank-start-body-fat confirm-then-refusal FIXED in `2293cafe`; two folded into commit 4 (its last bullet): the Journey's measurement log lists the day's standing value only, and the Overview's Since-start pill reads the page-level client record while the chart beside it reads the series.

- Migration (the next free number): drop the seven measurement columns on `check_ins`; drop `body_metrics` (mig 064's two policies go with it); delete `client_metric_entries` rows whose `metric_key` is a physique key (D3) and narrow its CHECK to the wellness keys; drop `clients.current_weight`, `current_body_fat_percentage`, `starting_weight` and `starting_body_fat_percentage` (D8, D4 revised — `CLIENT_SELF_COLUMNS` names all four). Grep every string-built column list at execution time before the push: a stale name is a PostgREST 400 that `tsc` cannot see, and the portal's is swallowed into an empty profile. `gen types`; diff.
- Code: `services/body-metrics-service.ts` deleted; `utils/metric-points.ts` and its test deleted; the merge code in `use-merged-metrics` deleted; the entries service's physique path deleted; the energy service's event stamping deleted; the audit constant renamed (D7). `npx knip` names the rest — delete what it lists, un-export what only its own file reads.
- Docs, current shape only: ARCHITECTURE → replace "body_metrics table" and "client_metric_entries table" with one "client_measurements table" section stating §2's rules (`measured_at` included); rewrite "The client's origin" per D4 revised (the start date is the origin, the baseline is derived, no start pair is stored); the "Client energy" and "Dual-write pattern" paragraphs (no cache; energy recomputes when a newest reading lands); the review-surface payload paragraph and the Journey tab row; CONVENTIONS §8's examples; TECHNICAL-DEBT → close "Client Metrics Log Extraction", add the D2 follow-up (wellness coach entries) and the girth-capture follow-up.
- PROD: push after `migration list --linked` against the prod ref; regenerate prod types and diff against the repo.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md before
planning. Plan for my review before writing anything.

This workstream changes the shape of body-measurement storage (plan §2); commits 1 and 2
have shipped (see §6). This commit deletes the old stores and rewrites the docs to the
new shape, so ARCHITECTURE and CONVENTIONS will contradict §2 in several places by
construction. Do not silently follow or silently override any of them: list each
contradiction with the doc line and what §2 says instead, and I will review before you
write. Everything you delete must be grepped at execution time, never taken from this
plan's list.

Job: Commit 3 of docs/MEASUREMENT-LOG-PLAN.md §6 — `chore(measurements): the old stores
go — columns, table, merge; docs`. Build exactly what that section lists: the migration
that drops the check-in columns, body_metrics, the physique entries and the four clients
columns; the body-metrics service, the merge layer, the entries service's physique path
and the event stamping removed; npx knip clean afterwards; ARCHITECTURE, CONVENTIONS and
TECHNICAL-DEBT rewritten to the current shape with no "used to" and no shipped/reverted
narrative. Do not delete this plan document — commit 9 does that.

Migration rules as in commit 2, DEV first. Before the PROD push: `supabase migration
list --linked` against the prod ref; show me the output and wait. Gates: npx tsc
--noEmit, npx eslint ., npx vitest run, npm run check:labels, npx knip, npm run
check:service-key, npm run check:rls. Commit directly to main. Then replace this
commit's STATUS line with SHIPPED, the hash and the date, and hand me a browser
smokelist: the review page for a weightless check-in, the Journey with a coach entry on
a check-in day, the client app after it, and the Overview status card.
```

### Commit 4 — `feat(measurements): a reading can be corrected or removed — never deleted`

**STATUS: SHIPPED `1872eeeb`, 2026-09-03.** Migration 160 on DEV and PROD (PROD listed 159 in sync with 160 pending before the push; its regenerated types are identical to the repo's; `check:rls` green on both, 46 tables). Deviations, each put to the owner at plan review and confirmed: `voided_by` is a real FK to `coaches` (SET NULL, partial index) so the list names the remover through an embed; `restore_measurement` takes no `p_actor`; a correction equal to the reading's standing value of ANY source writes nothing (the literal same-source rule 3 would have duplicated a check-in's value); a correction copies `measured_at`; the client's only live weight cannot be removed (RPC `last_weight` → 409 "correct it instead"), body fat can; the Remove dialog has no reason field (the route keeps `reason`); the series payload carries `readings` from a fourth, parallel table read (payload about 2× for DEV's largest client); correct/void/restore live in `services/measurement-edits-service.ts` while both INSERT paths stay in `measurements-service.ts`; `ReadingRemovalUnavailableError`'s sentence names the Journey's log and the "until commit 4" comments are gone. Both smoke findings are in: the log lists every reading (change against the previous day's standing value; removed rows muted, "Removed by … · date") and the Overview band reads its four figures from the series payload, pending while it loads. EXPLAIN under both roles kept the series index, so no partial twin. Proven on DEV through `next dev` by `scripts/measurement-edit-proof.ts` (46 checks over throwaway clients, the progress read under the client's own JWT); 15 mutations, 15 killed. Browser smoke PASSED 2026-09-03 (owner: all five items).

- Migration (the next free number): `voided_at timestamptz`, `voided_by uuid`, `void_reason text` on `client_measurements`; `client_measurements_live` gains its filter (`WHERE voided_at IS NULL`) and `client_current_measurements` (D8) is rebuilt on it; the RPC pair `void_measurement(p_id uuid, p_client_id uuid, p_actor uuid, p_reason text DEFAULT NULL)` and `restore_measurement(p_id uuid, p_client_id uuid, p_actor uuid)` — SECURITY DEFINER, `GRANT EXECUTE … TO service_role`, each sets or clears the three columns and nothing else, refuses a row already in the target state or outside `p_client_id` (the scope belt in SQL, since the route proves the coach owns the client and cannot prove the row does), returns the row's `metric_key` and whether it is the client's newest live row for that metric. The table grant stays `SELECT, INSERT`. If `EXPLAIN` under `SET LOCAL ROLE authenticated` shows the filter losing the series index, add a partial twin `WHERE voided_at IS NULL`. `gen types`; diff.
- `services/measurements-service.ts`: `voidMeasurement({ clientId, measurementId, actor, reason? })` and `restoreMeasurement({ clientId, measurementId, actor })` → their RPCs, then `recalculateClientEnergy` when the row was, or becomes, the newest weight or body fat — the same trigger `appendMeasurements` uses. The coach's measurement list reads the base table for its rows so removed ones render muted with `voided_by` / `voided_at`; every calculation and every client read goes through `client_measurements_live`. Every reader in the service reads `client_measurements_live`, so the filter lives in the view, once: `getMeasurementsForCheckIns` returns no reading for a voided check-in row, and the band's cell, the client's check-in detail and the AI prompt show their empty state for it; rule 3's unchanged-check reads live rows only, so re-logging a voided value writes a new row.
- Routes `POST /api/clients/[id]/measurements/[measurementId]/void` (zod `{ reason?: string }`, ≤ 200 chars) and `…/restore` (no body): `coachApiRateLimit` → `requireCSRFProtection` → `getAuthenticatedCoachId(request)` → `requireCoachOwnsClient` → service; audited as `measurement.void` / `measurement.restore` (`AUDIT_ACTIONS`, no value in metadata). The §2 security, load and performance review is run and reported — a new route and a new write path both trigger it.
- **Correction** (the owner's option 2): `correctMeasurement({ clientId, measurementId, value, actor })` in the same service reads the original row (scoped by `client_id`, so a foreign id is not found), then appends `{ metric_key, recorded_on, source_id }` copied from it, `source = 'coach_entry'`, `created_by = actor`, the new value — INSERT only, no RPC needed; rule 3's unchanged-check applies; energy recomputes when the new row is the client's newest. Because `getMeasurementsForCheckIns` reads the latest live row per (`source_id`, metric), a corrected check-in reading reaches the check-in's report, the band, the client's check-in detail, the AI prompt on regenerate, the Journey (rule 2) and every "now" surface, while the wrong row stays in history. Route `POST /api/clients/[id]/measurements/[measurementId]/correct`, the same chain, zod `{ value }` in canonical units checked against the `lib/constants.ts` bounds for the metric; audited as `measurement.correct` (no value in metadata).
- UI: the Journey's Physique measurement log gains three row actions. On a live row, **Edit reading** opens a one-field dialog in the viewer's unit (`useCanonicalInput`) and calls the correct route, and **Remove reading** sits behind the destructive-confirm dialog; on a removed row (muted, "Removed by … on …"), **Restore reading** is the only action. **Remove reading**'s dialog (`docs/newdesignsystem.md`): one sentence naming the reading and its date, ghost Cancel, primary Remove. When the row is the client's baseline (D4 revised) or their newest reading, the sentence says so — "This is the reading the since-start figures use" / "This is the current reading" — because removing it moves every figure built on it to the next reading, and an accidental removal should be caught before the click, not after. On success, three invalidators: the Journey series area, the Overview area, and the check-in detail area when the row carries a check-in stamp. Coach only.
- **Two findings from commit 3's smoke, folded in here (owner, 2026-09-03).** (1) The Journey's measurement log lists EVERY reading, not the day's standing value: a check-in's 91 kg sits under the coach's 90 kg logged the same day, and the chart keeps drawing the day-value — the rows read above gives the list this for free, since the log stops reading the series' day-values. (2) The Overview band reads every measurement number it shows — the headline, the Since-start pill and the goal chips' current and start — from the series payload it already fetches on every tab mount, never from the page-level client record. Today the pill and the chips read `client.currentWeight` / `startingWeight` off `useClient`, fetched once per page load with `revalidateOnFocus: false` and revalidated only by coach-side invalidators, while the chart's `useMeasurementSeries` re-mounts and refetches on every visit; a check-in submitted in the client's own session therefore reaches the chart on the next visit and the pill only on a full reload. One read for the four figures ends the split; the client record keeps serving every other consumer of `Client`.
- Tests, each with a mutation: the RPC refuses a double void and a row of another client (the request-level harness — the belt is SQL, and a vitest that mocks `supabaseAdmin` proves nothing here); a voided row leaves `getMeasurementSeries`, `client_current_measurements`, the check-in object and `GET /api/client/progress` — the last read under the client's JWT, so RLS plus the view are both exercised; the energy recompute fires only when the newest row was voided or a restored row becomes the newest; a restore refuses a live row and returns the row to the series, the current view and the check-in object; a re-logged value after a void writes a new row; a corrected check-in reading replaces the original in the check-in object, the day's value and "now" while the history read still returns both rows; a correction of another client's row is not found. `npm run check:rls`.
- Docs: ARCHITECTURE → "client_measurements table" gains rule 8 (correct and void); the audit constants.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md before
planning. Plan for my review before writing anything.

Commits 1–3 have shipped (see §6): every body measurement is a row in
client_measurements and nothing else stores one. This commit adds the three row actions of
D9: a reading can be CORRECTED — a new row carrying the original's day and stamp, so a
check-in's report reads the corrected value — REMOVED — hidden from every calculation
and from the client, muted on the coach's list, kept in the history — or RESTORED, all
through one RPC family, per my answer to D9 in §3. Where ARCHITECTURE or CONVENTIONS state a
rule that contradicts §2, D9 or this commit, do not silently follow it and do not
silently override it: list each contradiction with the doc line and what the plan says
instead, and I will review before you write. If D9 is blank, stop and ask.

Job: Commit 4 of docs/MEASUREMENT-LOG-PLAN.md §6 — `feat(measurements): a reading can
be corrected or removed — never deleted`. Build exactly what that section lists: the
migration (the columns, the live view, the RPC as the table's only UPDATE, the grant
unchanged); the three service functions with the energy recompute; the three coach
routes with the full chain and their audit events; the Journey's Edit, Remove and
Restore row actions, Remove behind the destructive-confirm dialog, with the three
invalidators; the tests, including the request-level ones, and their mutations;
check:rls; the docs.

Migration rules as in commit 2, DEV first; PROD after `migration list --linked`. Rules:
every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; gates: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip, npm run check:service-key, npm run check:rls. Commit directly
to main. Then replace this commit's STATUS line with SHIPPED, the hash and the date,
hand me a browser smokelist (correct a check-in's weight and open that check-in; remove
a check-in's reading and open it; restore it and open it again; remove the newest weight
and watch the Overview, the strip and the energy pair; remove then re-log the same
value).
```

### Commit 5 — `fix(logs): one definition of a logged day`

**STATUS: SHIPPED `e8c58cad`, 2026-09-03.** Deviations, each put to the owner at plan review and confirmed (decisions 1–6): a FOURTH reader found by grep — the adherence kernel's spine read behind the habits rail's Missed-versus-No-log — is switched too, so a day the client trained and ticked nothing now reads Missed; the single-client read lives in that kernel and reaches the header as `periodAdherence.loggedDates` over `dates` — no `services/logged-days-service.ts`, no top-level `daysLogged`, and the header's denominator is no longer a client-side day diff; the feed adds ONE read, the client's own measurement logs, so the alerts keep the fifth source (the plan said "adds none"); a wellness or nutrition row counts when it carries a reading, because the feed's view rows cannot tell an absent child from an empty one (DEV has zero all-null rows and the web cards refuse an empty save); the client wizard's "Week at a Glance" stays and is excluded by name in the scan; the full-week nutrition target's spine-presence test (`unloggedDates`) is recorded in TECHNICAL-DEBT, not fixed. Seven readers by grep, not three: the skip guard in `lib/attention-feed-helpers.ts` spelled its own union and reads the kernel now. DEV premise: 25 training-only days and 1 habits-only day without a spine row in the last 28. Nine mutations, nine killed. Pointer for `docs/TRAINING-COMPLETION-EXECUTION-PLAN.md`: its `lib/engagement-triggers.ts:62` site is now `isTrainingLogStatus` in `lib/logged-days.ts`. Browser smoke PASSED 2026-09-03 (owner, all four items: the header's 1/7, a training-only and a habits-only day counted, the two silence alerts clearing on a habit tick, the habits rail's Missed). Two observations from it, both pre-existing and deferred to the owner's separate attention-trigger plan: the logging-gap alert reports the OLDEST gap in the 28-day window rather than the open one, so a closed August gap stays on the feed until dismissed; and nutrition has no "missed against the prescription this week" trigger, only the off-target streak and the rest-day overeating pattern.

- `lib/logged-days.ts` (pure): `loggedDays({ wellness, nutrition, habits, training, measurements })` — five lists of `YYYY-MM-DD` dates in the client's calendar — returns the sorted set of days with any log the client made themselves. Coach entries and check-in submissions are not inputs (owner, 2026-09-02): the question is daily engagement, not the coach's work or the weekly report.
- `services/logged-days-service.ts`: `getLoggedDays(clientId, from, to)` reads the five sources — `wellness_logs`, `nutrition_logs`, `daily_habit_logs`, `session_logs` through their completed or partial `training_events` dated in the range, and `client_measurements_live` rows with `source = 'client_log'` (empty by construction until the client can log a weight, and listed now so it cannot be forgotten then). The attention feed's batch reader (all of a coach's clients, 28 days) reuses the reads it already makes (`services/attention-feed-service.ts:95-132`) and adds none.
- Readers switched: the check-in detail payload gains `daysLogged`, computed server-side, and the header (`components/clients/check-ins/check-in-detail-view.tsx:115`) reads it instead of `dailyLogs.length`; `lib/tracking-triggers.ts`' logging-gap trigger and `lib/engagement-triggers.ts`' no-engagement trigger both call the kernel — the latter's hand-rolled union (`:26-32`) and the comment explaining it go.
- Tests, each with a mutation: the kernel (a training-only day counts; a habits-only day counts; a wellness-only day counts; a day with only a coach entry or a check-in does not; a day outside the range does not); each reader; the scan `lib/logged-days-ownership.test.ts` — no file under `lib/*-triggers.ts`, `services/attention-feed*`, `services/check-in-details-service.ts` or the coach check-in tree counts `daily_logs` rows as a logged day or spells the union itself.
- Docs: ARCHITECTURE → "Daily Logs" states the definition — a logged day is derived from the five client sources; the spine row is the parent of the day-form (wellness, nutrition, the day note) and not the activity flag — and the attention-feed paragraph loses the workaround sentence; the review-surface header line reads "days with any client log".
- No migration, no writer touched, no wire removed (the detail payload gains one additive key).

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md before
planning. Plan for my review before writing anything.

"Did the client log today?" is answered three ways today: the check-in header and the
logging-gap alert count daily_logs spine rows, which only wellness and nutrition create;
the no-engagement alert unions the spine, the habit logs and the completed training
events itself (lib/engagement-triggers.ts:26-32 says why). This commit makes it one
derived definition (plan §3 D11). Where ARCHITECTURE or CONVENTIONS state a rule that
contradicts D11 or this commit, do not silently follow it and do not silently override
it: list each contradiction with the doc line and what the plan says instead, and I will
review before you write.

Job: Commit 5 of docs/MEASUREMENT-LOG-PLAN.md §6 — `fix(logs): one definition of a logged
day`. Build exactly what that section lists: the pure kernel with its five inputs; the
service and the attention feed's batch reader; the three readers switched; the tests,
the scan and their mutations; the docs. Coach entries and check-in submissions do not
count. No migration. Grep at execution time for every reader that answers the question;
do not trust the list.

Rules: every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; gates: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip, npm run check:service-key. Commit directly to main. Then replace
this commit's STATUS line with SHIPPED, the hash and the date, and hand me a browser
smokelist: a week with a training-only day and a habits-only day on the check-in header,
and the two alerts for a client who only trains.
```

### Commit 6 — `feat(measurements): the measurement log shows the selected metric`

**STATUS: SHIPPED `6ced90de`, 2026-09-03.** Decided: D12–D15 (owner, 2026-09-03, as recommended). Deviations, each put to the owner at plan review and taken: (1) `LogRow.metricName` and BOTH decorate lines (one per pane, not one) STAY — the section was not its only reader: commit 4's Edit and Remove dialogs read it at four sites; only the Metric column went. (2) The "Seen, not in scope" item is IN, folded into this commit (owner, 2026-09-03, asking for the best-practice shape rather than the parked one): the selected metric is Journey's second single-owner param, `?metric=` — read unconditionally, validated against the pane's own list so an unknown value or the other pane's metric derives to the pane default, written by the hero's switcher alone, deleted by a pane switch in the same navigation; the `focused` useState is gone. The page (an offset into an in-memory list, subordinate to the metric) stays local, as in the three history tables — the subject in the URL, the view of the subject local. (3) The page returns to 1 by the host's `key={focusedMetric.id}` — metric ids are unique across both panes, so one key replaces `key={tab}` — not by a derivation inside the section: the key is React's primary mechanism for resetting a child on a new subject, already in use here for the pane, and the derivation would have restored an old page on a return to a metric. (4) The section's prop is `Pick<MetricSummary, "id" | "name">`, the two fields it reads. (5) The empty state mirrors the chart's sentence exactly ("No Waist entries yet"); the pager noun is lowercase per D12's example ("12 weight entries"). Tests: the five section cases as listed, and a new host test (`metrics-tab-content.test.tsx`, router mocked, the real switcher and pane bar) carrying the URL contract and the page reset; the "wellness entry beside a physique reading" case was deleted as unreachable by construction. Ten mutations, ten killed. ARCHITECTURE: the Journey row AND the tab-param contract paragraph — two lines, not one. Follow-up, not built: under a single-metric log the tab-order tiebreak in both row builders' sorts is inert. Browser smoke PASSED 2026-09-03 (owner, all six items, the pasted `?metric=` deep link included).

What is there today, by grep (2026-09-03): the Physique and Wellness panes are one tree, `components/clients/metrics/metrics-tab-content.tsx`. The selected metric is `focused`, a `useState` reset by derivation on tab switch; `MetricHero`'s `MetricSwitcher` sets it, and the hero and `MetricProgressionSection` receive `focusedMetric`. The log does not: `MeasurementLogSection` (`components/clients/metrics/measurement-log-section.tsx`) receives `logRowsByTab[tab]` — every metric of the pane — built by `buildLogRows` (`utils/metric-derived-stats.ts`) from `useMergedMetrics`' points: newest-first by date, then definition order, with each row's delta already taken against its own metric's previous row. Its data is in memory before the log renders — the Physique series is ONE fetch of `GET /api/clients/[id]/measurement-series`, all seven metrics, no metric param (`getMeasurementSeriesPayload` → `getMeasurementSeries(clientId)`), and the Wellness rows come from the client-side merge. Paging is client-side in the section: `HISTORY_PAGE_SIZE` (10) over the journey rows, `DividerPager` printing "Showing X of Y entries" from `journeyRows.length`, the "Before start" rail unpaged beneath; the host remounts the section with `key={tab}` so the page resets on a pane switch. Six columns: Date, Day, Metric, Value, Change, Notes; empty message "No measurements logged yet". The hero's chips already count the selected metric's entries (`entryCount`), so today the hero says "12 entries" over a log that says "Showing 10 of 31 entries".

- `components/clients/metrics/measurement-log-section.tsx`: takes `metric: MetricSummary` beside `rows` and filters BOTH tables to `row.metricId === metric.id`; the pager's `total` is the filtered journey rows, so the count follows the selection; page state is keyed to the metric id by derivation (the `focused` idiom in the host — no effect, no reset call), so a switch returns to page 1 while `key={tab}` keeps handling the pane switch; the Metric column goes (D12) and the pager's noun names the metric (D12); the empty message names the metric (D15). `LogRow.metricName` (`metrics-view-types.ts`) and the `decorate` line that fills it (`use-merged-metrics.ts`) go with the column — the section is its only reader (grep at execution time).
- `components/clients/metrics/metrics-tab-content.tsx`: passes `metric={focusedMetric}`; nothing else moves. The same section serves the Wellness pane, so mood/energy/sleep/stress/soreness follow by construction.
- No route, service, hook or migration change (D14). No "all metrics" view (D13).
- Seen, not in scope: the selected metric lives in `useState`, not the URL — CONVENTIONS §7 says deep-linkable UI state lives in the URL (`?journey=` does, the metric does not). A `?metric=` param is its own commit if wanted.

| # | Decision | Recommendation | Why, and the alternatives |
|---|---|---|---|
| D12 | The METRIC column once the table is single-metric | **Remove it, and put the metric in the pager's noun** — "Showing 10 of 12 weight entries" — so the count and the metric read as one phrase and agree with the hero's "12 entries" chip | The switcher directly above names the metric and every Value cell carries its unit; a column repeating one word per row is noise. Alternatives: keep the column (redundant once single-metric); rename the rail per metric ("Weight log") — the rail's left side is the section's identity by the divider grammar, and it would change on every switch |
| D13 | An "all metrics" view | **None.** The switcher is the pane's one selector; the hero, the chart and the log describe one metric, and a coach wanting another picks it | Alternatives: an "All metrics" entry in the switcher — the hero and the chart cannot show "all", so the switcher would carry a state only the log honours; a second toggle on the log's rail ("This metric / All") — a second selector for the same page, more chrome beside the pager. If one is ever wanted, the rail toggle is the shape |
| D14 | Filter in the query or client-side | **Client-side, in the section** | The pane already holds every metric's rows: the series route returns all seven metrics in one fetch with no metric param (DEV's largest client: 370 day-values across all metrics, ~47 KB raw, under 10 KB compressed; weight alone 55), the wellness rows come from the client-side merge, and `buildLogRows` already builds rows per metric with per-metric deltas — the filter is one predicate over rows in memory, and the current data flow makes it free. A `WHERE metric_key = …` means a `?metric=` param, a per-metric SWR key and a fetch on every switch for a payload the pane already has; the switcher lists every metric with its latest value, so a per-metric fetch would need a second summaries read to keep it; and the route also serves the Overview chart, which needs weight and body fat. Alternative: the query filter — right only after commit 4, if every reading rather than the day's value reaches the log and the log's rows become the thing to page per metric server-side while the chart keeps day-values |
| D15 | The empty state for a metric with no readings | **"No {metric name} entries yet", the chart section's existing sentence, with no second call to action** | The chart's empty state directly above already offers "Log the first entry"; two identical CTAs on one screen is noise. Alternatives: keep "No measurements logged yet" (says nothing about the metric the coach just picked); repeat the CTA |

- Tests, each with a mutation, in `measurement-log-section.test.tsx` (rows of two metrics, weight and waist, plus a wellness case with mood): the log lists the selected metric's rows only and the pager counts only them (mutation: drop the filter — both fail); the "Before start" rail filters too (mutation: filter the journey rows only — a waist pre-start row leaks); a metric switch returns to page 1 — render on page 2 of twelve weight rows, rerender with waist (mutation: read the stored page without the id check); the empty state names the metric — weight rows only, metric = waist (mutation: the generic sentence); no Metric header cell (mutation: keep the column); a wellness metric filters the same way (mutation: the filter hardcoded to `"weight"`). `metric-hero.test.tsx` and `use-metrics-data.test.ts` are untouched.
- Docs: ARCHITECTURE → the Journey row of "Client page tab structure": the measurement log is the SELECTED metric's — the hero's switcher drives the hero, the chart and the log; the pager counts that metric's entries. CONVENTIONS and TECHNICAL-DEBT: nothing.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commit 6
before planning. Plan for my review before writing anything.

The Journey's Physique and Wellness panes select one metric at the top (the hero's
switcher), and the hero and the chart follow it; the measurement log at the bottom lists
every metric of the pane together and its "Showing X of Y entries" counts them all. This
commit makes the log the selected metric's, with the count to match. Where ARCHITECTURE
or CONVENTIONS state a rule that contradicts this commit, do not silently follow it and
do not silently override it: list each contradiction with the doc line and what the
plan says instead, and I will review before you write. D12–D15 are answered in the
commit's table.

Job: Commit 6 of docs/MEASUREMENT-LOG-PLAN.md §6 — `feat(measurements): the measurement
log shows the selected metric`. Build exactly what that section lists: the section takes
the selected metric and filters both of its tables to it, client-side, with the pager's
count following; the page returns to 1 on a metric switch by derivation, not an effect;
the Metric column, `LogRow.metricName` and its decorate line go; the empty state names
the metric; the tests and their mutations; the one ARCHITECTURE line. Grep at execution
time for the section, its host, the row builder and every reader of `metricName`; do not
trust the list.

Rules: every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; gates: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip, npm run check:service-key. Commit directly to main. Then replace
this commit's STATUS line with SHIPPED, the hash and the date, and hand me a browser
smokelist: switch metric on the Physique pane and read the log and its count; the same on
Wellness; a metric with no readings; a client with a reading before the start date; page
2 of one metric, then switch.
```

### Commit 7 — `feat(journey): the wellness series and the measurement log's fold — one route, five metrics, day-values; one row per day`

**STATUS: SHIPPED `83a0f061`, 2026-09-03.** Decided: D16 revised, D19, D20 and D21 (owner, 2026-09-03, as recommended); D17 moot, D18 reversed (commit 9). Deviations, each recorded in the commit body: `isWellnessKey` not built — no caller in this commit, and knip would name it; a second ARCHITECTURE clause edited beside the Journey row — rule 8's row-action sentence (Edit on the day's standing reading, Remove on any live reading); the entry's "DEV's largest ≈ 1,870 points" is 1,500 — the earlier count took rows with ANY reading, and soreness is null on most of Sam's rows. The kernel, keys and service are as listed; the wellness point is `{ date, value, id, recordedAt }`; the fold's folded live reading offers Remove only, Edit stays on the standing reading. Fifteen mutations, fifteen killed (kernel 4, service 1, route 3, hook 2, builder 3, section 3; two also fail `tsc`). DEV probe, every check holds: the fixture client 360 points on every key; Sam Kalepa mood 374, energy 374, sleep 373, stress 372, soreness 7 — each equal to the days with that column non-null, ascending and unique; the throwaway client's re-saved day changes its value in place and keeps its position, its backfilled day sits at its date though written last, four rows for four days, the cascade takes them with the client. EXPLAIN on DEV's largest client: a bitmap index scan on `idx_wellness_logs_client_date` and an in-memory sort, 374 rows, 19 buffers, 0.34 ms. Gates green (tsc, eslint 0 errors, vitest 366/3868, check:labels, knip, check:service-key). Browser smoke of the fold FOUND THE DEFECT that decided commit 8 (2026-09-03): the coach corrected 31 May's weight on the Journey, the check-in kept reporting its own stamped row and the fold offered Edit only on the day's lead. **The fold is REVERTED by commit 8** (owner: a reading is edited in place); the wellness half stands, and nothing renders it until commit 9.**

What is there today, by grep (2026-09-03): the Wellness pane's five metrics — mood, energy, sleep, stress, soreness — are the one part of the Journey still built in the browser from whole histories. `useMergedMetrics` reads `useAllClientCheckIns` (which pages the client's ENTIRE check-in list through `GET /api/clients/[id]/check-ins`, each page folding in the measurement log's stamped rows it does not need) and `useMetricEntries` (`GET /api/clients/[id]/metric-entries`, every coach entry), and `buildMetricPoints` (`utils/metric-points.ts`) merges them: one point per check-in — the check-in's stored weekly average, dated the submission day — plus one point per coach entry, a coach entry winning a same-day tie. The client's own daily wellness — `wellness_logs`, one row per client per day through the spine (`daily_logs` unique on client and date, the wellness row unique on its spine id), the five columns nullable, `date` and `updated_at`; the source of truth since Phase 6 and what a check-in's five averages are derived FROM at submit — is not read by the Journey at all; the Overview's five wellness cards and the Wellness tab read it, the tab through two routes that read `wellness_logs` alone. DEV's largest client: 374 wellness days (every row with a reading), 54 check-ins, 2 coach entries (the owner's, sleep and stress on 29 Aug) — the pane plots at most 55 points per metric where 374 days exist, and fetches 54 check-in rows to read five numbers each; no client on DEV has a day where a log and a coach entry coincide. The five keys have no home: they are spelled inline in `METRIC_ENTRY_KEYS` (`lib/metrics/metric-entry-definitions.ts`), again as ids in `WELLNESS_METRIC_DEFINITIONS`, as column predicates in `lib/logged-days.ts`, as a select string in the adherence service and in migration 159's CHECK. The physique measurement log (`components/clients/metrics/measurement-log-section.tsx` over `buildMeasurementLogRows`, `utils/measurement-log-rows.ts`) lists EVERY reading of the selected metric (commit 4's smoke finding 1): a coach's correction sits as a second row above the check-in's original, so a coach who clicked "Edit reading" sees an addition where they expected a change — the mechanism showing through instead of the result.

- `lib/wellness/keys.ts` (new): `WELLNESS_KEYS`, `WellnessKey`, `isWellnessKey` — the one home of the five, which are the five columns of `wellness_logs`; `lib/metrics/metric-entry-definitions.ts` spreads it into `METRIC_ENTRY_KEYS` until commit 9 takes the wellness keys out of the dialog (the code comments in `lib/wellness/keys.ts` and `lib/metrics/metric-entry-definitions.ts` still say "commit 8" — renumbered after they were written; commit 9 corrects them). The parallel of `lib/measurements/keys.ts`.
- `lib/wellness/day-values.ts` (new, pure, isomorphic): `wellnessDayValues(logs)` → `Map<WellnessKey, WellnessDayValue[]>` — one value per (metric, day) from the client's day-form rows: a non-null column is that day's reading, a null column no reading of that metric that day; ascending by `date`, every key present, empty when none. A row's `updated_at` rides along as `recordedAt` and orders nothing — an edited or backfilled day changes its one row in place and keeps its date. The parallel of `lib/measurements/day-values.ts`, with two callers by commit 11 (the coach's series service here, the client's progress read there), so both apps derive the same day from the same rows.
- `types/coach-overview.ts`: `WellnessSeriesPoint { date, value, id, recordedAt }` — no `source` and no `note`, since there is one source and it carries no notes — and `WellnessSeries = Record<WellnessKey, WellnessSeriesPoint[]>`; no `baseline`, no `startDate`, no `readings` (D20; a day holds one row and nothing is ever removed). `recordedAt` is the row's LAST write, unlike a measurement point's, and the type says so.
- `services/wellness-series-service.ts` (new): one paged read (`lib/paged-fetch.ts`) of `wellness_logs` for the client — `id, date, mood, energy, sleep, stress, soreness, updated_at`, ordered by `date` then `id`, served by `idx_wellness_logs_client_date` — through the kernel into the pure `toWellnessSeries`; `getWellnessSeriesPayload(clientId)`.
- `app/api/clients/[id]/wellness-series/route.ts` (new, D19): `coachApiRateLimit` → `requireCoachOwnsClient(clientId, request)` → the service, `Cache-Control: no-store` — the file shape of `measurement-series/route.ts`. The §2 security, load and performance review is run and reported: a read-only coach route on the full chain; one paged read, index-covered; the payload bounded by the client's logged days × 5 (DEV's largest ≈ 1,870 points).
- `hooks/use-wellness-series.ts` (new): a private key builder for `/api/clients/{id}/wellness-series`, `useWellnessSeries`, `useInvalidateWellnessSeries` matching the area — the `use-measurement-series.ts` shape, whose key builder is module-private like both reference implementations named in CONVENTIONS §7. With a hook test in the shape of `hooks/use-nutrition-calendar-events.test.ts`, which is also what keeps `npx knip` clean while nothing else imports the hook.
- **The fold** — `utils/measurement-log-rows.ts` and `measurement-log-section.tsx` (grep at execution time for `LogRow` in `metrics-view-types.ts`, the decorate line in `use-merged-metrics.ts`, `use-reading-actions.ts`, the Edit and Remove dialogs): ONE ROW PER DAY per metric, the reading in force — rule 2's winner among live rows, the id the day-values already carry — with its value, source, note and the Change column against the previous DAY's standing value (the chart's delta), and Edit reading and Remove reading on it. The day's other readings fold beneath it, collapsed, opened by a count on the row: a live superseded reading reads `corrected from 91 · check-in` when its stamp equals the standing reading's and `also logged 80.4 · client` otherwise (D21), each with Remove reading; a removed reading reads `removed 91 · check-in · by Sam · 3 Sep` with Restore reading. A day whose readings are all removed stays a muted row with Restore, as today. The "Before start" rail folds the same way. Edit is offered on the standing reading only. The pager counts rows, so its number and the hero's entries chip agree by construction, except for a day whose only readings are removed (a row here, no point there).
- Tests, each with a mutation. The kernel: a day with a log yields one point per non-null column with the row's id and last write; a null column yields no point for that metric and points for the others (mutation: emit 0); ascending by day whatever the input order (mutation: drop the sort); an edited or backfilled past day keeps its place and stays one point (mutation: sort by write time); every key present, empty when none (mutation: drop `soreness` from the list — `tsc` fails on the twelve-key module too). The service: the eight-column scoped read ordered by day then id (mutation: drop `soreness` from the select — `tsc` and the test); two pages; a thrown read error rather than an empty series. The route: the measurement-series route test's six cases. The hook: the area matched, another client rejected, the `measurement-series`, `daily-logs` and `history/wellness` keys rejected (mutation: widen the matcher to the client prefix), non-strings rejected, the drift guard (mutation: change the key builder), a null key without a client id. The fold, in `measurement-log-rows.test.ts` and `measurement-log-section.test.tsx`: a corrected check-in reading is one row reading 90 with `corrected from 91` folded (mutation: two rows); a coach reading over a client's on one day is one row with `also logged` (mutation: label it corrected); a removed reading on a day with a standing value folds with Restore; a removed-only day is a muted row; the Change column reads against the previous DAY's standing value (mutation: against the previous row); the pager counts days; Edit on the standing row only (mutation: on every row).
- A DEV probe script `scripts/wellness-series-proof.ts` beside `measurement-baseline-proof.ts`: for the fixture client and for Sam Kalepa, per metric, the payload's points against an independent count of days with that column non-null (raw date reads through PostgREST, not the assembly — there is no SQL path from a script) — printed, equal by construction, ascending and unique; then a throwaway client under the owner's coach row written through the app's own `upsertWellnessLog`: three days including a null column, one day re-saved (the same row — the value changes, the position does not) and one day backfilled after later days (it sits at its date), the row count staying the number of days; then deleted, the cascade proven. Every fixture number distinct. No coach entries anywhere: the store has none by commit 9.
- Docs, current shape only: ARCHITECTURE → the Journey row of "Client page tab structure": the measurement log is one row per day, the reading in force, the day's other readings folded beneath it as `corrected from`, `also logged` or `removed`. Nothing else — commit 9 rewrites the Wellness half.

| # | Decision | Recommendation | Why, and the alternatives |
|---|---|---|---|
| D16 | The wellness series' source | **The client's daily `wellness_logs`** — revised 2026-09-03: alone, no coach entries (D18 reversed) | Daily logs are wellness's source of truth (Phase 6): a check-in's five averages are derived from them at submit, and the Overview cards and the Wellness tab already read them. Plotting the derived weekly averages beside the daily values they were averaged from would count the same days twice, so the check-in points leave the Journey; the review page keeps showing each check-in's averages. Alternatives: serve today's series (weekly averages ⊕ entries) from a route — ends the pager but keeps a 55-point chart over 374 logged days; all three sources — a check-in's average is not a day's value |
| D17 | The day's value when the client's log and a coach entry share a day | **Moot** (2026-09-03): one source, no tie rule, no `updated_at` comparison | Superseded by D18's reversal |
| D18 | Coach-logged wellness entries | **REVERSED (owner, 2026-09-03): retired — commit 9.** A wellness score is the client's self-report; a coach can take a client's measurement, and a calibrated weigh-in can stand over a home one, but nobody can measure someone else's feeling | The original recommendation, keep them as a source, is the alternative: no product change, and a coach's number in the client's own series |
| D19 | One route or a sibling | **A sibling `GET /api/clients/[id]/wellness-series`**, the same payload shape, its own key and invalidator | `measurement-series` is shared with the Overview chart, which draws weight and body fat only; extending it would ship every wellness point to a surface that never draws one. Alternative: extend `measurement-series` with the five keys — one key for the Journey, and the Overview pays for it |
| D20 | "Since start" for wellness | **None: the hero's anchor stays the first point**, as it is today, and the payload carries no `baseline` | A mood as of the start date is not a figure a coach reasons about, and the physique baseline exists for goal maths wellness has none of. Alternative: derive the as-of-start reading for symmetry |
| D21 | The fold's label for a same-day reading that is not the day's value | **`corrected from` when its stamp equals the standing reading's (both non-null), `also logged` otherwise** | The store records a correction of a STAMPED reading — the correction carries the check-in's stamp — and nothing else: a coach's edit of their own unstamped entry and a second coach reading on one day are the same two rows, so the label states what the rows can state, and a coach's edit of their own entry reads `also logged 80.4 · coach entry`, which is true. Alternatives: one neutral word for both (`was 91`) — wrong for two true readings, where nothing was replaced; a stamp on every correction pointing at the original row — a change to what `source_id` means (the check-in id), a schema decision this commit does not take |

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commit 7
before planning. Plan for my review before writing anything.

The Journey's Wellness pane still builds its series in the browser: it pages the client's
whole check-in history to read five weekly averages per row and merges them with the
coach's entries, while the client's daily wellness logs — the source of truth — go
unread. This commit adds the wellness series as a route in the shape of the physique
one, over the client's daily log alone: one read, five metrics, day-values, a pure
kernel in lib/wellness that the client app reads too in commit 10, nothing reading the
route yet. It also folds the physique measurement log — one row per day, the reading in
force, the day's other readings collapsed beneath it — my decision, one commit. Where
ARCHITECTURE or CONVENTIONS state a rule that contradicts this commit, do not silently
follow it and do not silently override it: list each contradiction with the doc line and
what the plan says instead, and I will review before you write. D16, D19 and D20 are
answered in the commit's table; D17 is moot and D18 reversed (commit 9). Read my answer
to D21; if it is blank, stop and ask.

Job: Commit 7 of docs/MEASUREMENT-LOG-PLAN.md §6 — `feat(journey): the wellness series
and the measurement log's fold`. Build exactly what that section lists: the keys module
and the kernel in lib/wellness, the types, the service, the route on the full coach
chain, the hook with its key builder, area invalidator and test, the fold in the row
builder and the log section, the tests and their mutations, the DEV probe script, the §2
review, the one ARCHITECTURE line. Grep at execution time for the wellness_logs columns,
for every spelling of the five wellness keys, and for every reader of LogRow; do not
trust the list.

Rules: every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; gates: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip, npm run check:service-key. Commit directly to main. Then replace
this commit's STATUS line with SHIPPED, the hash and the date, and hand me a browser
smokelist for the fold: a corrected check-in reading, a coach reading over a client's on
one day, a removed superseded reading, a day whose only reading is removed, Restore, and
the pager's count against the hero's chip. Nothing renders the wellness series until
commit 9.
```

### Commit 8 — `feat(measurements): a reading is edited in place — edit replaces, add adds, the check-in follows its row; the fold goes`

**STATUS: SHIPPED `d4641a02` + `4e4b7009`, 2026-09-03.** Migrations 161 and 162 on DEV and PROD (PROD listed 160 in sync with 161 and 162 pending before the push; its regenerated types are identical to the repo's; `check:rls` green on both, 46 tables; PROD holds 0 measurement rows, so the backfill touched nothing there). **D23 REVISED at execution (owner, 2026-09-03): the day's value is the reading WRITTEN last, by `recorded_at`; an edit changes a reading's value and nothing else — never its place in the day and never which reading is the day's value; a coach who wants a different number to stand for a day logs a new reading. `updated_at` records the edit and orders nothing.** The first commit built the entry as written (`updated_at` deciding, the log ordered by last touch); the second reverses that under the revision, with migration 162 putting the two derived views and the series index back on `recorded_at` while `update_measurement` stays as built. Deviations, each put to the owner at plan review: the RPC returns a third column `changed` so the route audits nothing on a no-op; `updateMeasurement` takes no actor; the Edit dialog loses "The original stays in the log"; the last-weight refusal reads "Edit it instead"; the two derived views carry `updated_at`; the seeds and the perf script stamp it. The hero's three tag pills (unit, logging frequency, entry count) were removed on the owner's smoke — the count disagreed with the pager and served nothing — with `frequencyLabel` and `deriveFrequencyLabel`. Proven on DEV through `next dev` by `scripts/measurement-edit-proof.ts` (65 checks); 17 + 7 mutations, all killed; wire proofs — the fixture client's 14 recordings byte-identical across both commits and every check-in and comparison recording byte-identical across the first (Sam Kalepa's later recordings differ only because a check-in was submitted and the smoke ran between them), the two series recordings shape-kept with `readings[].updatedAt` added. Gates green on both commits. Browser smoke PASSED 2026-09-03 (owner: all five items, item 2 read as "the edited row stays where it was and the day's value is whichever reading was logged last").

Why (found on DEV 2026-09-03, Sam Kalepa's 31 May): the coach corrected the day's weight to 72 on the Journey, the check-in kept reporting 80, and the goal strip read 78. The strip was right — it reads today's newest reading, by commit 1's rule. The other two disagreed because a correction was a NEW stamped row and a check-in read only rows carrying its stamp: the coach's edit went to an unstamped coach entry, the day's lead and the only row commit 7's fold offered Edit on, so the check-in's row never changed. One store, two rules about which row a reading is, and an edit that did not do what it said. The owner had asked for editing in place from the first review and was argued out of it; that argument was wrong about what a coach sees.

- Migration (the next free number at execution time — 161 unless the training-completion plan pushed first; commit 10 takes the one after): `updated_at timestamptz` on `client_measurements`, BACKFILLED from `recorded_at` for every existing row — never from the default, or every old reading would read as touched today and every day's value would shift — then `NOT NULL DEFAULT now()`; the series index re-cut as `(client_id, metric_key, recorded_on DESC, updated_at DESC)`; one RPC `update_measurement(p_id uuid, p_client_id uuid, p_value numeric)` — SECURITY DEFINER, `GRANT EXECUTE … TO service_role`, the shape of the void pair: refuses a row outside `p_client_id` (`not_found`) and a removed row (`voided`), writes nothing when the value is unchanged, sets `value` and `updated_at = now()` and nothing else, returns the row's `metric_key` and whether it is now the client's newest live reading of that metric; `client_current_measurements` and `client_baseline_measurements` rebuilt to order by `recorded_on DESC, updated_at DESC` (DROP+CREATE loses grants — the REVOKE/GRANT block is repeated). The table grant stays `SELECT, INSERT`: every UPDATE the table sees is one of the three RPCs. `gen types`; diff. PROD after `migration list --linked`.
- Rule 2 becomes "the day's value is the reading written or edited last" (D23): `lib/measurements/day-values.ts` compares `updatedAt` and breaks a tie by id; `MeasurementReading` gains `updatedAt`; every row read selects it (`READING_COLUMNS`, the readings list, the client's progress read). Rule 3 stands for adds (an identical add writes nothing); an edit to the same value writes nothing and audits nothing.
- `services/measurement-edits-service.ts`: `correctMeasurement` → `updateMeasurement({ clientId, measurementId, value, actor })` → the RPC, then `recalculateClientEnergy` when the row is the client's newest weight or body fat; `appendCorrection` in `measurements-service.ts` is deleted. `getMeasurementsForCheckIns` keeps "the latest live row per stamp and metric", now by `updated_at`: correction rows written before this commit stay ordinary stamped readings and still resolve.
- Route `PATCH /api/clients/[id]/measurements/[measurementId]` (zod `{ value }` in canonical units against the metric's bounds; the full chain — `coachApiRateLimit` → CSRF → `getAuthenticatedCoachId(request)` → `requireCoachOwnsClient`; 404 foreign, 409 removed, 400 out of range) replaces `POST …/correct`, audited as `measurement.update` (metric and date only); void and restore untouched. `use-reading-actions.ts` calls it; the Edit dialog is unchanged.
- The log returns to ONE ROW PER READING (commit 4's list): `buildMeasurementLogRows` builds a row per reading, newest day first and within a day the most recently touched first, the Change column against the previous day's standing value; Edit and Remove on every live row, Restore on a removed one; `LogRow.folded`, `FoldedLogRow`, `FoldKind`, the fold toggle, the fold tests and the three fixtures' `folded: []` go. Nothing beneath a row, no count beside a value. A day with two genuine readings is two rows, because two were added.
- Tests, each with a mutation. The RPC, through the request-level harness (the belt is SQL): an edit keeps the id, day, source and stamp and changes the value; a foreign row is not found; a removed row is refused; an unchanged value writes nothing. The kernel: an edited older reading becomes the day's value; a later add wins over an earlier edit; a tie breaks by id. The check-in fold: the edited stamped row is what the check-in object reports, and `GET /api/client/progress` under the client's JWT reads it. The energy recompute fires only when the edited row is the newest. The PATCH route's chain. The log section: a day with two readings is two rows, Edit on both, no fold. `scripts/measurement-edit-proof.ts` revised — its correction scenarios become in-place ones; the void and restore scenarios stay. `npm run check:rls`.
- Wire proofs: `GET /api/client/check-ins`, `…/[id]` and `GET /api/check-in/[id]` byte-identical; `GET /api/client/progress` and the series route shape-compared (a day whose older reading was edited changes value by design).
- Docs, current shape only: ARCHITECTURE → "client_measurements table" rules 1, 2 and 8 (a reading is edited in place and removed by a mark, never deleted; the day's value is the reading written or edited last; Edit on any live reading keeps its stamp so a check-in follows its row), the writers and readers paragraphs, the Journey row (every reading a row, Edit on each); CONVENTIONS §8's timestamps exception loses `client_measurements` (it carries `updated_at` now). This plan's §2 rules 1–3, D1, D9 and D21 carry their superseded notes.
- Browser smoke: edit a check-in's weight from the Journey and open the check-in; edit a coach entry on a day that also has a check-in reading and watch the day's row; add a second reading on a day; remove and restore; the goal strip against today's reading.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commit 8
before planning. Plan for my review before writing anything.

From this commit a reading is EDITED IN PLACE: edit replaces the row's value, add adds a
row, editing a check-in's reading edits its own row — stamp kept — so the check-in's
report follows, and delete deactivates with Restore, as built. The day's value is the
reading written or edited last (D23). This reverses the plan's append-only rules 1–3 for
edits, D1's correction rows, D9's correction path and commit 7's fold (D21), by my
decision on 2026-09-03. Where ARCHITECTURE or CONVENTIONS state a rule that contradicts
this commit, do not silently follow it and do not silently override it: list each
contradiction with the doc line and what the plan says instead, and I will review before
you write.

Job: Commit 8 of docs/MEASUREMENT-LOG-PLAN.md §6 — `feat(measurements): a reading is
edited in place`. Build exactly what that section lists: the migration (updated_at
backfilled from recorded_at, the update RPC, the two views and the index on updated_at);
the service and the PATCH route replacing correct; the kernel on updated_at; the log back
to one row per reading with Edit on every live row and the fold deleted; the tests
including the request-level ones and their mutations; the proof script revised; the wire
proofs; the docs. Grep at execution time for every reader of the fold and of
appendCorrection; do not trust the list.

Migration rules as in commit 2, DEV first. Before the PROD push: `supabase migration
list --linked` against the prod ref; show me the output and wait. Rules: every fixture
number distinct; cp backups before mutating, never git stash or git checkout --; gates:
npx tsc --noEmit, npx eslint ., npx vitest run, npm run check:labels, npx knip, npm run
check:service-key, npm run check:rls. Commit directly to main. Then replace this
commit's STATUS line with SHIPPED, the hash and the date, and hand me a browser
smokelist: the five items in the entry.
```

### Commit 8b — `feat(check-ins): the review reads the check-in's day — the reading then, the goal then, the clock then`

**STATUS: SHIPPED `2f5390f4`, 2026-09-03.** Three decisions at plan review, each put to the owner: (1) a check-in older than every goal version shows NO goal — the review composes its input from the version in force alone, with no `clients.*` mirror leg (the entry's fallback to the live goal through the mirror would have put today's goal on every pre-versioning page; the owner's "none of the data is real" settled it — every real client's goal history is complete in `client_goals`, so no version then means no goal then); (2) the AI prompt's previous-check-ins block gets the same `upTo` at both callers, the Regenerate route and the submit-time trigger — it read the client's latest five whichever check-in was regenerated; (3) the drift note is not gated. `goalIsCurrent` is "the version judged is unsuperseded". The harness (`scripts/check-in-as-of-proof.ts`, 28 checks on DEV through `next dev`) runs on Sam Kalepa's 31 May — older than every version, so it proves the stamped-row rule and the no-goal page, not the superseded-version path — and on a throwaway client with two versions, four check-ins and two nutrition versions for that path, the trend bound (wire-observable: a later check-in's higher weight flips `isOnTrack` if the bound is dropped) and the drift version. Nineteen mutations, nineteen killed. DEV premise at execution: Sam's first goal version is 4 June, so no check-in of his sits inside a superseded version; the browser smoke's item 1 needs one made (change the goal in the sheet, then open the 3 Sep check-in). Browser smoke OWED.

What is there today, by grep (2026-09-03): `services/comparison-service.ts` builds `goalProgress` from `client.currentWeight` / `currentBodyFatPercentage` (the client's newest reading of ANY date), `getCurrentGoals` (the live goal), `today` = the client's today for the pace window and `daysRemaining`, `getClientCheckIns(clientId, { limit: 10 })` — the client's LATEST ten check-ins whichever one is under review — for the trend, and the nutrition version covering the client's today for the drift note. So a May check-in reviewed in September reads September's weight, September's goal, September's clock, a trend over check-ins that came after it, and September's plan: on DEV, Sam Kalepa's 31 May check-in read "88 → 70 · 8 kg to go" from a 78 logged that afternoon. The band's deltas, the KPI ribbon, the wellness section, the habits and nutrition figures and the AI prompt already read the check-in's own rows and period.

- **One clock.** `at` = the check-in's `created_at` (an instant); `day` = its client-local day, `getTodayDateStringInTimezone(client.timezone, new Date(createdAt))` — the conversion the writer used for its rows' `recorded_on`.
- **The reading then.** `getReadingsAsOf(clientId, day, checkInId)` in `services/measurements-service.ts`: per metric (weight, body fat), the check-in's own live stamped row when it has one, else the newest live reading dated on or before `day` (`recorded_on DESC, updated_at DESC` — rule 2 at a date, the same as-of rule the baseline view applies at the start date). Fed to `deriveGoalProgress` as the position; the baseline (the start-date reading) is unchanged — the origin does not move.
- **The goal then.** `getGoalAsOf(clientId, at)` in `services/client-goals-service.ts`: the version with `effective_from <= at AND (superseded_at IS NULL OR superseded_at > at)`; a check-in older than every version (history from before goal versioning) falls back to the live goal through `toClientGoalInput`'s mirror leg. `resolveEffectiveGoal` runs with `today = day`.
- **The clock then.** `daysRemaining` / `weeksRemaining` from `day` to that version's deadline, so `computeGoalPace` judges the rate required FROM THEN.
- **The trend then.** The ten check-ins up to and including the reviewed one — `getClientCheckIns` gains an `upTo` bound (`created_at <= at`); never the latest ten.
- **The drift note then.** The reading then against the base weight of the nutrition version covering `day` (`getNutritionPlanForDate(clientId, day)`), not the version covering today.
- **The wire.** `GET /api/check-in/[id]/comparison` keeps its shape; `goalProgress` gains `goalIsCurrent: boolean` (additive) — true when the version in force then is still the live one — and `check-in-goal-strip.tsx` renders its footer "Goal met — set new goals" only when it is true, so a historical page never invites replacing a goal already replaced. The client's check-in detail shows no goal progress; RN untouched.
- **The scan.** `lib/goals/goal-progress-ownership.test.ts` is rewritten to the new rule: on the review path the position comes from `getReadingsAsOf` and nothing else — never `client.currentWeight`, never a bare check-in field; the Overview's callers are unchanged.
- Tests, each with a mutation: `getReadingsAsOf` — the stamped row when present, else the reading before the day, never one after it, a removed row ignored (mutation: the newest of any date); `getGoalAsOf` — a superseded version in force at `at`, the live one when nothing superseded it, the boundary at `effective_from`, the pre-versioning fallback (mutation: the live goal); the comparison — for a check-in with a later goal and later readings, position, goal, deadline and pace are the check-in's (mutation: today's); the trend bounded to the reviewed check-in (mutation: the latest ten); `goalIsCurrent` false for a superseded version and the footer hidden (mutation: always true); the drift note against the version covering `day`. The request-level harness for one historical check-in on DEV — Sam Kalepa's 31 May, against a goal changed after it.
- Docs, current shape only: ARCHITECTURE → "Goal progress and pace" (the Overview reads now; a check-in review reads its day — the reading then, the goal then, the clock then), the review-surface paragraph's goal-strip sentences, "Effective goal resolution" (the comparison resolves the as-of version). This plan: rule 6 and the §4 row carry the note.
- Browser smoke: a May check-in after a goal change (goal, deadline, position and pace are May's); the newest check-in the day after a coach adds a reading (the strip keeps the check-in's number, the Overview shows the new one); a weightless check-in (the reading before it); a check-in whose goal has since been met and replaced (no "Set new goals" on the old page).

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commits 8
and 8b before planning. Plan for my review before writing anything.

A check-in review must reflect where the client was at that time (my decision,
2026-09-03): the goal strip reads the check-in's own reading — or the reading before it
when it carried none — the goal version in force on its day, days remaining from that
day, the ten check-ins up to it and the nutrition version covering it. The Overview and
the Journey keep reading now. This reverses commit 1's "position reads the client
record's current reading" for the review page only. Where ARCHITECTURE or CONVENTIONS
state a rule that contradicts this commit, do not silently follow it and do not silently
override it: list each contradiction with the doc line and what the plan says instead,
and I will review before you write.

Job: Commit 8b of docs/MEASUREMENT-LOG-PLAN.md §6 — `feat(check-ins): the review reads
the check-in's day`. Build exactly what that section lists: the two as-of reads, the
comparison on the check-in's clock, the trend bound, the drift note, the additive
`goalIsCurrent` and the footer, the scan rewritten, the tests and their mutations, the
harness proof on a historical check-in, the docs. Grep at execution time for every
reader of the comparison payload and every caller of getClientCheckIns; do not trust the
list. No migration.

Rules: every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; gates: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip, npm run check:service-key. Commit directly to main. Then replace
this commit's STATUS line with SHIPPED, the hash and the date, and hand me a browser
smokelist: the four items in the entry.  
```

### Commit 8bb — `feat(nutrition): the deficit runs from the day the plan takes effect — the drawer picks the date, the preview and the save compute from it; the goal start date goes`

**STATUS: SHIPPED `6870c595` + `e3802f45`, 2026-09-04 (the first commit late on 2026-09-03 local). Migration 163 on DEV and PROD — PROD pushed 2026-09-04 after `migration list --linked` and a probe (0 rows; DEV carried a value on 222 of 256 rows), PROD's regenerated types byte-identical to the repo, `check:rls` 46/46 on both. Owner decision 2026-09-03, CLOSED: the date a nutrition plan takes effect is picked in the drawer before the save, the calculated calories and macros follow it as it changes, and the version that lands on the calendar carries the per-day numbers that reach the goal by its deadline from that day; the goal's own start date is gone — the column, both schemas and their refine, the sheet's field and the deadline floor composed from it, the resolver's `startDate` AND its `today` input (six callers dropped it), `ReadyCalcInputs.startDate`, the two pass-throughs, the merge line, the row type, the seed's column. Deviations, each put to the owner at plan review: the effective date is DERIVED (`effectiveFromPick ?? calcInputs.today`) rather than seeded, so the default follows the client's day across their midnight and can never land as a past-date 400; the request always carries the date; a saved plan resets the pick to today (D27); the footer keeps the dialog's past-date guard as one clause; no TECHNICAL-DEBT entry existed for the refine's hole; `docs/CLIENT-PORTAL-EXECUTION-PLAN.md` Session 7.8 is left as history. Sixteen mutations, sixteen killed; every gate green after the review. Browser smoke PASSED 2026-09-04 (owner, all five items). Lettered 8bb so nothing renumbers.**

Why (found 2026-09-03, walking the review's clock through the calculator): the deficit is spread from the day of the calculation to the deadline, whatever effective date the coach picks. A plan saved today to begin tomorrow loses one day of its window, which is noise; a cut queued four weeks out understates its deficit by four weeks. The goal's start date was the intended fix — `calculateBaselineCalories` counts from the later of it and today — so a coach had to set the same date on the goal and on the plan, and nothing checked they agreed. ARCHITECTURE already rejected the goal start date as the client's origin ("The client's origin"); after this commit nothing reads it.

What is there today, by grep (2026-09-03): the drawer's two pickers, protein per kilo and diet type, are `settings` in `hooks/use-nutrition-builder.ts` rendered by `components/clients/nutrition/builder/nutrition-settings-form.tsx`; the live preview is `generateNutritionPlan({ ...calcInputs, ...settings })`, recomputed on every change, over the coach GET's `calcInputs` (`startDate` = the goal's start date, else the client's today; `today`). The effective date is asked AFTER the coach clicks Generate, in `ApplyDateDialog` from `drawer-footer.tsx`, sent as `effectiveFrom` (`lib/validations/nutrition.ts`), and consumed by `createNutritionPlan` (the version's window) and the events regenerate only — the orchestrator's calculated branch (`services/nutrition-plan-orchestrator.ts`, `handleCalculatedPlan`) computes from `calcInputs` alone, so the preview and the save both spread the deficit from today. The goal start date's readers: both zod schemas (`lib/validations/client-goals.ts`, `components/clients/overview/use-client-profile-edit.ts`) with the start-before-deadline refine and its documented partial-update hole; the sheet's Goal start field and the deadline floor composed from it (`components/clients/details/details-groups.tsx`); `ClientGoalInput.startDate`, `EffectiveGoal.startDate` and the today fallback in `lib/goals/resolve-effective-goal.ts`; `toClientGoalInput`; `ReadyCalcInputs.startDate` in `services/nutrition-calc-inputs.ts`; `services/client-journey-service.ts` and `services/comparison-service.ts`, which pass it in and read nothing back; `updateGoals`' merge; `ClientGoal` / `ClientGoalRow`. Nothing on the Overview, the goal strip, the Journey or any client wire reads it.

- **First commit — `feat(nutrition): the deficit runs from the day the plan takes effect`.** The date is a third drawer setting, "Starts on", beside the two pickers: `settings.effectiveFrom`, seeded with the client's today from `calcInputs.today`, `min` today, on the client's calendar like the server's past-date belt. The absorb warning the dialog carries today — saving on or before a queued change's start replaces that version (migration 144) — moves inline under the field, from `nutritionData.scheduledFor`. The footer's Generate button saves directly with `settings.effectiveFrom`; `ApplyDateDialog` leaves the footer (grep its other users at execution time; delete the component if none). The preview hands the picked date to the calculator as the window start — `generateNutritionPlan({ ...calcInputs, ...settings, startDate: settings.effectiveFrom })` — so the calories and macros move the moment the date changes; the calculator already clamps the start to today. The save computes from the same date: `handleCalculatedPlan` passes `startDate: body.effectiveFrom ?? clientToday` over `calcInputs.startDate`, so what the coach saw is what lands from that day. Manual mode computes nothing and is untouched. The request schema already carries `effectiveFrom`; no wire changes.
  Tests, each with a mutation: the field renders with today as its floor (mutation: no floor); the preview changes when the date does (mutation: drop the override); the request carries the picked date (mutation: omit it); the server computes from it — a date three weeks out yields a steeper deficit than today's (mutation: from today); the preview and the save agree for a future date, one pure calculator over the same inputs (mutation: the save from today); the absorb warning shows on or before the queued date and not after (mutation: never). `drawer-footer.test.tsx` loses the dialog.
- **Second commit — `chore(goals): the goal start date goes`.** Migration (the next free number at execution time — the training-completion plan and 8d also take numbers): `ALTER TABLE public.client_goals DROP COLUMN IF EXISTS goal_start_date`. `gen types`; diff; PROD after `migration list --linked`; `npm run check:rls`. Deleted, each grepped at execution time: `goalStartDate` in both schemas and the cross-field refine with its documented hole; the sheet's Goal start field and the deadline floor composed from it (the deadline keeps today as its floor); `ClientGoalInput.startDate`, `EffectiveGoal.startDate` and the fallback (`resolveEffectiveGoal`'s `today` parameter goes with it if no caller needs it afterwards — grep); `toClientGoalInput.startDate`; `ReadyCalcInputs.startDate` (the calculator's `startDate` is the effective date, handed in by the orchestrator and the drawer alone); the two pass-throughs; `updateGoals`' merge line; `ClientGoal.goalStartDate` and the row type; the seeds' mentions; the 8d entry's start-date clauses are already gone from this plan. `calculateBaselineCalories`' comment about "future-dated goals" is rewritten: the window starts at the later of the plan's effective date and today.
  Tests, each with a mutation: the resolver without a start (mutation: re-add the fallback); the sheet without the field; the schemas without the refine; the calculator's window from the effective date (mutation: from today).
- Docs, current shape only: ARCHITECTURE → "client_goals table" (the field list and the refine sentence lose the start date), "Effective goal resolution" (no `startDate`), "Nutrition plan versions" (`effective_from` is when the numbers take effect AND the day the deficit is spread from), "Builder flows" → Nutrition (the drawer's Starts on setting; the preview and the save compute from it), "The client's origin" (the rejected-origin sentence loses its goal-start-date entry). TECHNICAL-DEBT: the partial-update date hole closes by deletion. This plan: the §4 rows and 8d's entry carry the change.
- Browser smoke: open the drawer, pick a date three weeks out and watch the calories and macros move, save, and read the calendar from that day; save on today and compare with the previous numbers; pick a date on or before a queued change and read the absorb warning; the sheet has no Goal start field; a deadline before today is still refused.

| # | Decision | Recommendation | Why, and the alternatives |
|---|---|---|---|
| D26 | The save-time date dialog | **Removed.** The date is a drawer setting and the Generate button saves directly; the absorb warning moves inline under the field | Alternative: keep the dialog as a confirm step showing the chosen date — a second click for information the coach just set |
| D27 | The field's default when a queued change exists | **Today**, like every other setting, with the inline warning naming the queued date | Alternative: default to the queued change's date so a re-save keeps the queue — hides that the save would absorb it, which is the thing the warning exists to say |

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commit 8bb
before planning. Plan for my review before writing anything.

The nutrition deficit is spread from the day of the calculation to the deadline, whatever
effective date the coach picks, so a plan queued to start later understates its deficit;
the goal's own start date was the only lever and had to be typed twice. Two commits under
one letter (my decision, 2026-09-03): first, the effective date becomes a drawer setting
before the save, the calculated calories and macros follow it as it changes, and the save
computes from it; second, the goal start date goes — the column, both schemas and their
refine, the sheet's field, the resolver's fallback and the calculator input. Where
ARCHITECTURE or CONVENTIONS state a rule that contradicts this commit, do not silently
follow it and do not silently override it: list each contradiction with the doc line and
what the plan says instead, and I will review before you write. Read my answers to D26
and D27; if either is blank, stop and ask.

Job: Commit 8bb of docs/MEASUREMENT-LOG-PLAN.md §6, in order: `feat(nutrition): the
deficit runs from the day the plan takes effect`, then `chore(goals): the goal start date
goes`. Build exactly what that section lists: the Starts on setting with its floor and
inline absorb warning, the dialog gone, the preview and the save computing from the
picked date, the tests and their mutations; then the migration, the deletions — each
grepped at execution time for a surviving reader, never taken from this plan's list —
the tests, the docs. Grep at execution time for every user of ApplyDateDialog and every
reader of goalStartDate, goal_start_date and the goal path's startDate; do not trust the
list.

Migration rules as in commit 2 for the second commit, DEV first. Before the PROD push:
`supabase migration list --linked` against the prod ref; show me the output and wait.
Rules: every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; gates: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip, npm run check:service-key, npm run check:rls. Commit directly to
main, one commit each. Then replace this entry's STATUS line with SHIPPED, both hashes
and the date, and hand me a browser smokelist: the items in the entry.
```

### Commit 8c — `feat(check-ins): a submitted check-in closes its period — one day rule, the logged-day lock goes`

**STATUS: NOT STARTED. Owner decision 2026-09-03, CLOSED: a check-in is a record of its period, so once it is submitted nothing in that period can change. The trigger is the submission, nobody picks a day: a day is locked when it falls on or before the last submitted check-in's `period_end`, or after the client's today. Every day between the last check-in and today is editable, logged or not — the "past logged → locked" rule is REPLACED, not kept beside this one. No reopen. No same-day warning (clients check in in the evening, after logging; a client with a session due that day logs it first). After the lock, only the coach changes anything about a closed period, and only readings, from the Journey's measurement log (commit 8). Lettered, so nothing renumbers. Independent of 8b and of the training-completion plan. No migration.**

Why (found on the commit-8 smoke, 2026-09-03): a check-in freezes its figures at submit — `workouts_completed`, `adherence_percentage`, `nutrition_days_on_target`, the five wellness averages, `period_snapshot` — and the client's check-in list and detail read those frozen columns. The coach's review does not: its ribbon, wellness section, habits and nutrition read the period's logs live. So when a client backfills a day after submitting, the coach's review moves while the client's copy stands still — two readers, two answers. The day rule allows the backfill: a past day never logged is editable for ever. The frozen figures also disagree with the review on an UNCHANGED week, because the stored `workouts_completed` counts full sessions only while the ribbon counts full and partial; that is a counting-rule defect, owned and fixed by `docs/TRAINING-COMPLETION-EXECUTION-PLAN.md` commit 4 ("every done-count includes partials by construction"), not by this commit. Once both are in, the stored figures cannot go stale and cannot be counted differently, and that plan's commit 4b builds one report assembly for both check-in views, reading the headline numbers from the check-in row — that plan holds the entry, self-contained, since this document is deleted by commit 12.

What is there today, by grep (2026-09-03): `lib/daily-log-permissions.ts` — the pure rule `canEditDay(date, loggedStatus, clientTimezone)` (today editable; past never-logged editable; past logged locked; future locked), `DayLogStatus`, `DailyLogResourceType`, `DayLockedError`; `services/daily-log-permissions-service.ts` — `getDayEditState` reads the client's timezone and the resource's CHILD table (`RESOURCE_TABLE`, a per-habit `habitId` narrowing) to decide "logged", `assertCanEdit` throws the error and returns `loggedStatus` so the routes answer 201 on a first log and 200 on an update, `assertCanEditTrainingDay` takes `loggedStatus` from the event's log status; the four client write routes that call it — `daily-logs/[date]/nutrition`, `daily-logs/[date]/wellness`, `habits/log`, `training/events/[eventId]/log` (through `services/training-log-service.ts`); the client-side readers of the pure rule — `app/client/habits/page.tsx`, `app/client/nutrition/page.tsx`, `app/client/wellness/page.tsx` (each deriving `loggedStatus` its own way), `components/client-portal/training/set-tracker.tsx` and the check-in form's `components/check-in/training-session-checklist.tsx` (the `check-in-context` route ships the timezone for it); `components/client-portal/day/locked-day-notice.tsx` with its `past-logged` reason; the client calendar move, `services/training-event-layout-service.ts`, with its own past-day policy — "a target before today is allowed only when that day has no logged workout", read from `session_logs`, beside the week bound, the pinned logged session and the status-agnostic occupancy check; comments citing the old rule in `lib/validation-helpers.ts`, `lib/validations/daily-habit.ts`, `lib/validations/daily-log-cards.ts`, `app/api/client/day-summary/route.ts`; the docs — ARCHITECTURE "Date-edit permissions", `docs/CLIENT-PORTAL-REDESIGN.md` "Date edit rules" (and its line 340), `CLIENT-APP-REFERENCE.md` line 179 (`403` day locked). Twelve test files cover the old rule. The check-in stores `period_start` / `period_end` (`resolveCheckInWindow`), one check-in per period (migration 156).

- **The rule, once.** `canEditDay(date, closedThrough, clientTimezone)` in `lib/daily-log-permissions.ts`: `closedThrough` is the last submitted check-in's `period_end` on the client's calendar, null when the client has never checked in; a day is editable when `date <= today` and (`closedThrough` is null or `date > closedThrough`). Pure and client-safe as today. `DayLogStatus` is deleted; `DailyLogResourceType` and `DayLockedError` stay (the error carries `closedThrough` so the UI can name the check-in's date).
- **The wrapper.** `getDayEditState(clientId, date)` reads the client's timezone and `closedThrough` — `max(period_end)` over the client's `check_ins`, one indexed query — and returns `{ editable, closedThrough }`; `assertCanEdit({ clientId, date, resourceType })` throws `DayLockedError`. The child-table read, `RESOURCE_TABLE`, the `habitId` option and `assertCanEditTrainingDay` are deleted; the training log path calls `assertCanEdit` with `resourceType: "training"`. **D24 (below)** decides whether the routes keep answering 201 on a first log; if they do, the one existence read they need moves into those routes and no longer touches the lock.
- **The client calendar move.** `training-event-layout-service.ts` gains the rule's fourth line — no move may have a source or a target day on or before `closedThrough` — and loses the `session_logs` backfill read and its "That day already has a logged workout" refusal: a logged day already holds a completed event, which the status-agnostic occupancy check refuses on its own, and a logged session is already pinned. The week bound stays.
- **The wire and the UI.** `closedThrough` rides on `GET /api/client/me` (additive — the one client-level read every page already holds, refetched after a submit); the five client-side readers call the rule with it and stop deriving `loggedStatus` for the lock; `locked-day-notice.tsx`'s reason becomes `closed-period` with the copy "This day is in the check-in you sent on 31 Aug and can't be changed" (`today-no-plan` stays). The `loggedStatus` field on the wellness payload is a fact about the day-form and stays on the wire (the RN contract is additive-only); only its use for the lock goes. The check-in form's training rows are all editable within the open period; after the submit the period is closed.
- **Nothing on the coach side changes.** The coach never logs for a client; the coach's own past-day rule on the calendar (a logged day's prescription is frozen) is a different rule and stays. The coach edits a closed period's readings from the Journey, and only those.
- **Deleted with the old mechanism, grep at execution time:** `DayLogStatus`; the `loggedStatus` parameter and its three page-side derivations (`logMap.has(habit.id) ? "logged" : "never-logged"`, `nutrition?.source === "log"`, `wellness?.loggedStatus` for the lock); `RESOURCE_TABLE` and the child read (subject to D24); `assertCanEditTrainingDay`; the layout service's backfill read and refusal; the `past-logged` reason and its copy; the old rule's comments in the four helper and validation files; every test of the old rule.
- Tests, each with a mutation. The pure rule: today editable; a day on `closedThrough` locked (mutation: `<` for `<=`); a day after it and before today editable whether or not it is logged (mutation: read a logged flag); the future locked; a null boundary opens every past day; "today" in the client's zone. The wrapper: one query for the boundary, none for a child row (mutation: read the child); a client with no check-ins. Each of the four write routes: 403 inside the closed period, 200 or 201 after it (mutation: drop the assert). The layout service: a source inside the closed period refused, a target inside it refused, no `session_logs` read (mutation: keep the old read). The five readers: locked state from the boundary alone. The notice copy. `docs/CLIENT-PORTAL-REDESIGN.md` and the RN contract updated.
- Docs, current shape only: ARCHITECTURE → "Date-edit permissions" (the one rule, the boundary, the calendar move), "Check-in System" (a submitted check-in closes its period; the frozen figures can no longer go stale), `docs/CLIENT-PORTAL-REDESIGN.md` "Date edit rules", `CLIENT-APP-REFERENCE.md` (the 403 sentence, the `closedThrough` field). This plan's §4 row.
- Browser smoke: edit a past LOGGED day inside the open period (now allowed); submit a check-in, then try a workout log, a nutrition entry, a wellness entry and a habit tick on a day inside its period (each locked, with the notice naming the check-in's date); a day after the period (open); move a workout in the client's week into the closed period (refused) and within the open week (allowed); the coach's review after the submit against the client's check-in card (equal once training-completion commit 4 is in; until then the partial-count difference is that plan's).

| # | Decision | Recommendation | Why, and the alternatives |
|---|---|---|---|
| D24 | The 201-versus-200 answer on a first log, which today reads the child row the lock no longer needs | **Keep the codes.** The RN contract is additive-only and `201 {sessionLogId}` is documented; the existence read moves into the routes that answer 201 and stops being part of the lock | Alternative: answer 200 always and delete the read — one query fewer per write, but a contract change the RN app must be checked against first |

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commit 8c
before planning. Plan for my review before writing anything.

A submitted check-in closes its period (my decision, 2026-09-03): a day is locked when
it falls on or before the last submitted check-in's period_end, or after the client's
today; every day between the last check-in and today is editable, logged or not. This
REPLACES the "past logged → locked" rule — delete it and the code that served it, do
not keep it beside the new rule. No reopen, no same-day warning, no migration. Where
ARCHITECTURE, CONVENTIONS or docs/CLIENT-PORTAL-REDESIGN.md state a rule that
contradicts this commit, do not silently follow it and do not silently override it:
list each contradiction with the doc line and what the plan says instead, and I will
review before you write. Read my answer to D24; if it is blank, stop and ask.

Job: Commit 8c of docs/MEASUREMENT-LOG-PLAN.md §6 — `feat(check-ins): a submitted
check-in closes its period`. Build exactly what that section lists: the pure rule on
the boundary, the wrapper reading the boundary and nothing else, the four write routes
and the training log path on it, the calendar move's fourth line and the deletion of its
backfill read, closedThrough on /api/client/me, the five client-side readers and the
notice, the deletions listed under "Deleted with the old mechanism", the tests and
their mutations, the docs. Grep at execution time for every reader of canEditDay,
assertCanEdit, getDayEditState, DayLogStatus, loggedStatus and LockedDayNotice; do not
trust the list.

Rules: every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; the RN wire is additive-only; gates: npx tsc --noEmit, npx eslint ., npx
vitest run, npm run check:labels, npx knip, npm run check:service-key. Commit directly
to main. Then replace this commit's STATUS line with SHIPPED, the hash and the date, and
hand me a browser smokelist: the items in the entry.
```

### Commit 8cc — `feat(nutrition): the events run as far as the training does — the horizon derives from the client's programs`

**STATUS: NOT STARTED. Owner decision 2026-09-04, CLOSED: nutrition events are generated to the LATER of the window they already reach (the scope's anchor + 8 weeks) and the furthest end date among the client's live training programs — LIVE meaning not soft-deleted and not archived, WHATEVER ITS DATES, so a program queued to start in three days counts exactly like one already running. Derived at generation time, never stored. The floor is what makes the change safe: it can only ever generate MORE than today, never less, so nothing that works now can break. Between programs, no events past the horizon is CORRECT and stays that way (a client who finishes reviews with their coach, or takes the time off logging entirely, and the coach regenerates when they restart); the wall belongs at the end of the block, not at an arbitrary eight weeks that sometimes lands mid-block. Lettered so nothing renumbers. Independent of 8c and of 8d. No migration, no wire change, no coach-facing control.**

Why (found 2026-09-04, tracing the day-target read while planning 8c): `calculateNutritionEndDate` (`services/nutrition-event-service.ts:373-377`) is a fixed 8 weeks from the anchor, and nothing tops the rows up — the platform has no scheduler. A nutrition version is open-ended (`effective_until IS NULL` on the newest), so it says the client is prescribed those targets indefinitely while the rows stop. Eight weeks after whatever last generated them the wall arrives at today and then moves with it: the client's day screen shows no target (`getPlanTargetForDate` resolves a missing row to null, and ARCHITECTURE's level-3 template fallback is unbuilt), a food log written on such a day snapshots that null PERMANENTLY so the day never counts toward adherence, and the coach's nutrition data table — which reads events back to today — shows blanks for the most recent weeks. The client's own program card disagrees with all three, because `buildDailyTargetsFromPlan` falls back to the covering version's weekday grid. What hides it is ordinary activity: a nutrition save, a placement, an amendment, a move, a duplicate, an event delete, a surplus edit and the client's own week rearrangement all cascade. A stable client whose coach edits nothing for two months walks into it MID-BLOCK, which is the only place the wall is in the wrong place.

Considered and rejected at plan review (owner, 2026-09-04). **A read-time fallback** in `getPlanTargetForDate` to the covering version's weekday grid — ARCHITECTURE's unbuilt level 3: it puts a branch on every day read to cover a state the horizon can simply never create, and it fixes one reader of three, because the coach's calendar and the coach's data table read `nutrition_events` directly and would still render blanks, re-opening a coach/client split. **A coach-facing end-date picker** in the nutrition drawer: it asks a coach to name an end for something that has no end (a version runs until the next save replaces it), and a date picked short of the block re-opens the hole with the coach believing they handled it. **Ending the nutrition VERSION at the program's end** (the first reading of the owner's question): a day past the end would have no version covering it, `assertHasActivePlan` would refuse the client's food log outright, and the gap that migration 144's close-and-insert made unexpressible would become routine. The horizon is a materialisation range; the version window is unchanged and stays open.

- **The rule, once.** The forward window is `max(calculateNutritionEndDate(anchor), the furthest live program end)` — the 8-week window the code already computes from the scope's own anchor, extended by a program end and never shortened by one. `calculateNutritionEndDate` keeps its name and its constant as the FLOOR; a program end only ever extends it. A program whose `effective_until` is NULL does not extend it — an unbounded training window would generate unbounded nutrition rows — and a program that has already ended cannot pull the window below the floor, because it is a `max`.
- **The read.** A sibling in `services/training-service.ts`, beside `getNextFutureTrainingPlan` and `getTrainingPlansOverlapping`: client-scoped, `deleted_at IS NULL`, `status <> 'archived'`, the greatest non-null `effective_until`, and NO date predicate of any kind. A program that has not started yet is exactly the case this rule exists for, so `effective_from` is never filtered on; a program that has already ended cannot pull the window below the floor, because the window is a `max`. Those two exclusions are load-bearing and both existing readers document why — the copy that forgot `status <> 'archived'` re-surfaced retired plans as the client's current program. A third hand-rolled select without them is the defect this bullet exists to prevent.
- **Derived at generation time, never stored.** `resolveScopeDates` is pure and synchronous today and becomes async, taking the `clientId`. That is the point: a coach who sets nutrition up before placing the program keeps the floor until the placement's own cascade runs, and the horizon then stretches on its own; an amendment that extends the block does the same. Nothing caches the number, and no column holds it.
- **The delete range still equals the generate range.** Both come from the one `resolveScopeDates` call inside `regenerateFutureNutritionEvents`, and they must stay that way: the module's own comment records the bug where a cascade anchored earlier than the anchor that wrote the rows deleted a tail it never rebuilt.
- **The frequent paths are untouched.** `{kind:"dates"}` scopes — a move, a duplicate, an event delete, a surplus edit, and the client's own week rearrangement — still write only the dates they name. Only the `{kind:"from"}` scopes (whole-program placement, the plan-clear DELETEs, the amendment floor, a nutrition save) see the wider range, and all four are infrequent coach actions.
- Tests, each with a mutation. The horizon: a client with no live program gets exactly the floor (mutation: drop the floor); a program ending inside the floor does not shorten it (mutation: take the program end unconditionally); a program ending beyond it extends to that day (mutation: keep the constant); a program with a null end does not extend it; an archived or soft-deleted program does not extend it (mutation: drop the exclusions); a program that starts in three days and has not begun still extends the window (mutation: require the program to cover today); the furthest of two live programs wins, so a block queued behind the current one is covered (mutation: take the covering program's end). The generation: a placement made after a nutrition save extends the events on its own cascade (mutation: resolve the horizon once and cache it); the delete range equals the generate range (mutation: give the delete its own anchor). One end-to-end proof that a 16-week block generates nutrition events through its final day.
- Docs, current shape only: ARCHITECTURE → "Nutrition & Training Events" (the forward window is the later of the floor and the furthest live program end, resolved per generation) and the "Training → Nutrition cascade" paragraph where the fixed 8 weeks is named; `TECHNICAL-DEBT.md`'s nutrition-cascade "stale tail" entry, narrowed rather than closed. This plan's §4 row.
- Browser smoke: on a client with a live block longer than 8 weeks, save a nutrition plan, then open the coach's nutrition calendar at the block's final week (targets present) and the client's day screen on a date past 8 weeks (a target, and a food log that stores it); queue a second block and confirm the placement's cascade covers it without a nutrition save; on a client with no training program, confirm the horizon is unchanged from today.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commit 8cc
before planning. Plan for my review before writing anything.

Nutrition events stop 8 weeks after whatever last generated them, and nothing tops them
up. A nutrition version is open-ended, so it says the client is prescribed those targets
indefinitely while the rows stop: eight weeks on, the client's day screen has no target,
a food log written that day snapshots a null target permanently and never counts toward
adherence, and the coach's data table shows blanks for the recent weeks. My decision
(2026-09-04): the generation horizon is the LATER of the window the code already reaches (the
scope's anchor + 8 weeks) and the furthest end date among the client's live training programs,
derived at generation time and never stored. Live means not soft-deleted and not archived,
whatever its dates: the read carries NO date predicate, so a program queued to start in three
days extends the horizon exactly like one already running. The floor means this can only ever generate more than today, never less. Between
programs, no events past the horizon stays correct. Do NOT add a read-time fallback, do
NOT add a coach-facing end-date picker, and do NOT end the nutrition version at the
program's end — all three were considered and rejected, and the entry says why.

Job: Commit 8cc of docs/MEASUREMENT-LOG-PLAN.md §6. Build exactly what that section
lists: the horizon rule with its floor, the live-program read carrying the deleted_at and
archived exclusions the two existing readers document, resolveScopeDates deriving the
horizon per call, the delete range still equal to the generate range, the tests and their
mutations, the docs. Grep at execution time for every caller of calculateNutritionEndDate
and resolveScopeDates; do not trust the list. Where ARCHITECTURE or CONVENTIONS state a
rule that contradicts this commit, do not silently follow it and do not silently override
it: list each contradiction with the doc line and what the plan says instead.

Rules: every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; no migration and no wire change; gates: npx tsc --noEmit, npx eslint ., npx
vitest run, npm run check:labels, npx knip, npm run check:service-key. Commit directly to
main. Then replace this commit's STATUS line with SHIPPED, the hash and the date, and hand
me a browser smokelist: the items in the entry.
```

### Commit 8d — `feat(goals): a goal lives once and resolves by date — date-ranged versions, one atomic write with change detection, the profile copy gone; the drawer reads the goal for the day the plan takes effect; the block target goes`

**STATUS: NOT STARTED. Owner decisions 2026-09-03 and 2026-09-04, CLOSED: a goal is stored in ONE place, the `client_goals` table, and its versions are DATE-RANGED like nutrition's since migration 144 — `[effective_from, effective_until]` on the client's calendar, close-and-insert with the absorb rule, one open row per client, no overlaps. A coach may set a goal to start on a future day: the row is inserted at once with its future `effective_from` and the version in force is closed at `new_start − 1` (timeline chaining), so the queued goal is invisible to every reader until its day. A version that has governed days is never edited, only replaced from a date (replace only — a goal is a decision, and a changed decision is a new one from a date; a reading is a fact, which is why commit 8 chose edit in place for readings). "Current" resolves BY DATE everywhere through the one `coversDate` predicate training and nutrition use: the Overview, the Journey, the client wires and the intake sync read the version covering the client's today; the check-in review judges against the version covering the check-in's day — winner takes all, a goal starting on the check-in's day included — and the nutrition drawer reads the version covering the day the plan takes effect. A client never sees a queued goal before it starts. The two goal columns copied onto `clients` (and the never-mapped `goal_deadline` beside them) are dropped; setting a goal is one all-or-nothing write; a save that changes nothing writes nothing and mints no version. The goal SHAPE does not change — one version holding a weight target and a body-fat target with one shared deadline is the product (a row-per-goal shape was declined 2026-09-03). Three commits under one letter — the first carries the goal migration, DEV then PROD after a probe of BOTH databases; the second needs none; the third (owner, 2026-09-04) drops `client_phases.target_weight` with its own migration: a block is a look-back lens and reads the goal version covering it, and a stored block target was a second target concept the nutrition never solved against — a pace toward a number no plan was built for. Runs after 8bb (shipped: the old `goal_start_date` is gone, and a version's `effective_from` is a different thing — when the version takes effect, not where a deficit is spread from); independent of 8c. The client wires keep their shape; the coach goals wire changes shape (dates in place of instants).**

Why: two diseases. The first is the one D8 cured for weights. Migration 9 put `goal_weight` and `goal_body_fat_percentage` on `clients`; migration 60 created the versioned table, backfilled every goal into it, and left the columns as a fallback for "a client whose goal predates the table". Since then every goal save has been three autocommitted round trips — supersede the live version, insert the new one, copy the two numbers back onto the profile row — with the copy's UPDATE logged-and-swallowed, so the copy can silently fall behind; the read switch in `toClientGoalInput` (`currentGoals?.goalWeight ?? client.goalWeight`) then decides which store to believe; and `updateGoals` supersedes-and-inserts on EVERY call, so change detection lives in the callers and a details-sheet save with the goal untouched mints a version and an audit event. A failed insert after the supersede leaves the client with no live version, which every reader takes as maintenance. The second is that a goal goes live the moment it is saved. A coach who sets up a client's next block on a Sunday — the goal, the plan, the program — can queue the plan (migration 144) and the program (placement from a date) but not the goal: a bulk target set four weeks ahead of the end of a cut shows on the Overview, the goal strip and the client app at once, the drift banner fires against the running plan, and a check-in submitted before the intended day is judged against a goal the client is not yet working to. The old `goal_start_date` did not fix this — it only shifted the deficit window while every reader treated the goal as live — which is why 8bb dropped it and why this commit does not bring it back.

What is there today, by grep (2026-09-04): `client_goals` carries `effective_from TIMESTAMPTZ` stamped at the write, `superseded_at`, `idx_client_goals_active_unique` (`client_id WHERE superseded_at IS NULL`), `idx_client_goals_client_effective`, and mig 064's two policies (`clients_view_own_goals`, `coaches_view_client_goals`); DEV holds 256 rows, 225 live, PROD 0. The reads: `getCurrentGoals` (the live version — called by `services/nutrition-calc-inputs.ts`, the coach nutrition GET, `services/client-journey-service.ts`, `app/api/clients/[id]/goals/route.ts`, `services/client-service.ts`, `services/intake-review-service.ts`), `getGoalAsOf` (the review's instant read, commit 8b, `services/comparison-service.ts`), `getGoalsHistory` (superseded versions, `…/goals/history` and `goal-history-popover.tsx`); the writer `updateGoals` and its callers — the details sheet through `PUT …/goals`, `updateClient` for goal fields carried by `PATCH /api/clients/[id]`, the metrics PUT, `createClient`, the intake sync — each carrying their own change detection or none. The copy is read or written by twelve non-test files — `lib/mappers.ts` (the two columns onto `Client.goalWeight` / `goalBodyFatPercentage`, and the intake mapper), `lib/goals/resolve-effective-goal.ts` (the mirror legs of `toClientGoalInput`; the deadline has none by the 0b.1 decision), `services/client-goals-service.ts` (the mirror UPDATE), `services/client-service.ts`, `services/client-portal-service.ts` (`/api/client/me`), `services/client-portal-progress.ts` (a string-built select naming the columns, and `goalWeight` / `goalBodyFatPercentage` on the `/api/client/progress` wire read STRAIGHT OFF THE COPY), `services/intake-review-service.ts` (reads the copy to decide what the questionnaire may fill), `components/client-portal/metrics/goals-section.tsx`, `lib/sentry-scrubber.ts`, `scripts/seed/generate.ts`, `scripts/seed-scale-client.ts`, `scripts/perf-baseline.ts`; 23 test files name the fields. `client_goals.primary_goal` is inert but an unconditional INSERT key (ARCHITECTURE: remove the code first, then the column). The nutrition drawer takes its `calcInputs` — the live goal's weight and deadline among them — from the coach GET once at load; the orchestrator resolves the live goal for the save; `detectGoalDrift` compares the open plan version's snapshot with the live goal. The check-in proof harness `scripts/check-in-as-of-proof.ts` (28 checks) exercises `getGoalAsOf` by instant.

- **First commit — `feat(goals): a goal lives once and resolves by date`.** Migration (the next free number): `effective_from` becomes a DATE on the client's calendar (the backfill casts each row's instant in its client's timezone, the check-in backfill's convention); `superseded_at` becomes `effective_until DATE` (the successor's `effective_from − 1`, NULL for the last version); two versions of one client on one day collapse to the later one (the absorb rule applied to history — the probe counts them on DEV first; PROD has no rows); `CHECK (effective_until IS NULL OR effective_until >= effective_from)`; `idx_client_goals_active_unique` becomes `(client_id) WHERE effective_until IS NULL`; a gist exclusion on `(client_id, daterange(effective_from, effective_until, '[]'))` so overlapping windows are physically impossible (`btree_gist` is already installed by 144). One RPC `set_client_goals_atomic(p_client_id uuid, p_set_by uuid, p_today date, p_effective_from date DEFAULT NULL, p_goal_weight numeric DEFAULT NULL, p_goal_body_fat_percentage numeric DEFAULT NULL, p_goal_deadline date DEFAULT NULL, p_notes text DEFAULT NULL, p_fields text[] DEFAULT NULL)` — SECURITY DEFINER, `GRANT EXECUTE … TO service_role`, `p_effective_from` coalesced to `p_today`: a caller-cooperative belt refuses `effective_from < p_today` (a version cannot start in the past — backdating would rewrite what a submitted check-in was judged against) and `goal_deadline < effective_from` (a target cannot precede its own start; this is the cross-field rule 8bb deleted, reborn where both values are known — it replaces the route's coach-today deadline bound, since `effective_from` is never before the client's today); `p_fields` names the keys the caller is setting and the body merges them over the OPEN version (the latest saved, the one the sheet seeds from); the 144 branches — no open version → insert; a queued version whose start is on or after `p_effective_from` → hard-delete it (fully replaced, never effective) and continue; `p_effective_from` after the covering version's start → close it at `p_effective_from − 1` and insert; `p_effective_from` equal to the covering version's start (a same-day re-save) → update it in place; and change detection first: merged targets equal to the open version's and `p_effective_from` inside its window → `changed = false`, nothing written. Returns the version now covering `p_effective_from` and `changed`. Then, after the probe, `DROP COLUMN` `goal_weight`, `goal_body_fat_percentage`, `goal_deadline` on `clients`, and `primary_goal` on `client_goals` with its code. The probe, on DEV and PROD before the file is written: clients with a non-null copy and no version (expected 0 — migration 60 backfilled every goal that existed; if any exist, the migration inserts a version for them first), and same-day sibling versions. `gen types`; diff; `npm run check:rls`.
  **The reads, three roles, one module** (`services/client-goals-service.ts`, mirroring `nutrition-plan-service.ts`): `getGoalForDate(clientId, date)` (the version covering a day, through `coversDate`), `getNextFutureGoal(clientId, today)` (the earliest queued), `getOpenGoal(clientId)` (the latest saved — the sheet's seeds), `getGoalHistory` (versions with `effective_until < today`, newest first, bounded). `getCurrentGoals` and `getGoalAsOf` go: the live surfaces call `getGoalForDate` with the client's today (`resolveNutritionCalcInputs`' prefetched goal, the coach nutrition GET, the journey, the goals route, the intake sync, `createClient` / `updateClient` through the writer), and the review calls it with the check-in's day — the same day its readings and its nutrition version already key on, so its three as-of reads share one key. `goalIsCurrent` becomes "the version judged covers the client's today". `lib/goals/goal-progress-ownership.test.ts` keeps its readings clause and its goal clause becomes a read-path scan: nothing outside the goals service reads `client_goals`. `resolveEffectiveGoal` and `toClientGoalInput` lose the mirror legs; `Client` carries NO goal fields and no view exists (**D25**, re-decided below); `/api/client/me` and `/api/client/progress` compose their goal fields from `getGoalForDate` with the client's today, so both wires keep their shape byte for byte; the string-built selects lose the column names; the intake sync asks the goal read whether the client has a goal.
  **The writer.** `updateGoals(clientId, goals, setBy, { effectiveFrom? })` calls the RPC and nothing else and returns `{ goal, changed }`; the routes audit `goal.create` only on a real change. The goals PUT computes the client's today for `p_today` (the nutrition POST's precedent) and drops its coach-today deadline bound. Callers drop their own change detection (grep: `use-client-profile-edit.ts`, `updateClient`, the intake sync). `updateGoalsSchema` gains `effectiveFrom` (format only, optional).
  **The sheet and the Overview.** The Goals & energy group gains "Starts on" — the coach's pick, else the client's today (the client record's timezone, `getTodayDateStringInTimezone`), `min` that day, with the absorb warning under it naming the queued version's date (from the goals read's `next`) — the drawer's field, the same words; the group seeds from the open version. The goals GET returns `{ goal, next }` (additive); the Overview's band shows the covering goal and a "New goal from d MMM" line for a queued one with a Remove action (**D29**); the history popover lists ended versions. The check-in proof harness moves to the day model.
- **Second commit — `feat(nutrition): the drawer reads the goal for the day the plan takes effect`.** The calculator is pure and already runs in the browser; what changes with the date is the resolved inputs. A coach route `GET /api/clients/[id]/nutrition/calc-inputs?date=` (full coach chain; the shared `resolveNutritionCalcInputs` with `getGoalForDate(clientId, date)`) behind an SWR hook keyed on the drawer's `effectiveFrom` — `hooks/use-nutrition-calc-inputs.ts`, key builder and invalidator in one module, invalidated with the goals area — so a change of the Starts on date refetches the goal weight and deadline for that day and the preview recomputes; the preview and the numbers it feeds render pending while the read is in flight (a `TextSkeleton` in the real element, never an empty field). The coach GET stops shipping `calcInputs`. The orchestrator resolves the goal for `body.effectiveFrom ?? clientToday` through the same resolver, so preview and save agree over the same inputs for the same day. `detectGoalDrift` compares the open plan version's snapshot with the goal covering that version's OWN `effective_from` — the drawer's `GoalSummary` line shows the same goal — so a plan queued for a day inside the next goal reads that goal, and a goal queued after a plan was built for its day raises the banner. Manual mode is untouched.
- **Third commit — `chore(blocks): the block target goes — a block reads the goal version covering it`.** Migration (the next free number): `ALTER TABLE IF EXISTS public.client_phases DROP COLUMN IF EXISTS target_weight`, DEV and PROD probed first for rows carrying one — a fact stated in the commit, never a justification. The block card's goal line and its pace readout read the goal version covering the block's reference date — today for a current block, its final day for a past one, its first day for a future one, the facts route's convention — through `getGoalForDate`, and the pace is `computeGoalPace` over that version's deadline from the reference date, so the block card and the goal strip cannot disagree; the block's own pace kernel (`derivePace`) goes. A past block whose window spans a goal change shows the version covering its final day. The client journey's `targetWeightKg` keeps its name on the wire (additive-only) and is sourced the same way, null when no version covers the block; the blocks PUT drops the field from its schema, so a payload still carrying one is stripped, never refused. Deleted, each grepped: the column and its mapping (`types/client-blocks.ts`, `services/client-blocks-service.ts`, the journey's source), the block form's target field and its unit input, `block-chain-payload.ts`'s carry, the subtab's pace readout, `derivePace`, the seeds' mentions. Tests, each with a mutation: the block card reads the version covering its reference date (mutation: the version covering today); a past block spanning a goal change shows the version covering its final day (mutation: its first day); the journey wire's `targetWeightKg` derives, and is null with no version (mutation: always null); the PUT stores no target. Docs: ARCHITECTURE "Journey blocks" (the entity loses `target_weight`; a block's target is the goal version covering it; the "nothing computes from" sentence) and the facts-route paragraph.
- **Deleted, each grepped at execution time:** the mirror UPDATE and its swallow; the three `clients` columns and every mapping of them; the mirror legs; `Client.goalWeight` / `goalBodyFatPercentage`; the callers' change detection; `primary_goal` and its INSERT key; `superseded_at`, `getCurrentGoals`, `getGoalAsOf` and the cursor-charset belt on its instant; the route's coach-today deadline bound; `calcInputs` on the coach GET; the seeds' and the scrubber's mentions; `client_phases.target_weight` and everything the third commit lists.
- Tests, each with a mutation. The RPC through the request-level harness (the belt is SQL): an unchanged payload writes nothing and mints no version (mutation: always insert); a changed target from today closes the covering version at yesterday and inserts, and the history gains exactly one row; a future start inserts a queued version and closes the covering one at `start − 1`, and the covering one still answers today (mutation: open the queued one); a save on or before the queued start replaces it and leaves one open row; a same-day re-save updates in place; a start before the client's today is refused (mutation: drop the belt); a deadline before the start is refused; an overlap is refused by the constraint; a foreign client id is refused; `set_by` recorded. The reads: `getGoalForDate` picks the covering version at a boundary — the day a version starts is its day (mutation: `<` for `<=`); the review judges a check-in submitted inside a period whose goal changed mid-period against the version covering its day, and a check-in from before the first version shows no goal (mutation: the live goal); `goalIsCurrent` false for a version since closed. The resolver without a mirror leg (mutation: re-add it). The selects name no dropped column (a scan over the string-built lists — a stale name is a PostgREST 400 that `tsc` cannot see). The sheet: Starts on floored at the client's today; the absorb warning on or before the queued date and not after; an untouched save writes nothing. The drawer: the inputs refetch when the date changes and the preview follows the goal for that day (mutation: key the read on nothing); preview and save agree for a day inside a queued goal (mutation: the save from the live goal); the drift banner keys on the plan's own effective date (mutation: today's goal). `npm run check:rls`.
- Wire proofs: `GET /api/client/me`, `GET /api/client/progress` and `GET /api/client/journey` byte-identical for a client with a goal version (`scripts/wire-proof-measurements.ts` gains the journey route; the fixture client is the subject), and a client with a queued goal shows the covering one on all three; `GET /api/clients/[id]/goals` shape-compared (dates, `next`).
- Docs, current shape only: ARCHITECTURE → "client_goals table" rewritten as date-ranged versions (the mirror of "Nutrition plan versions": one store, the branches, the belt, one atomic write with change detection inside it, `primary_goal` gone), "Effective goal resolution" (one read by date; the review by the check-in's day; `goalIsCurrent`), "Goal progress and pace" (**D28**), "The client's origin" (the rejected origins: a plan's or a goal version's `effective_from`), "Read switch fallback" (the goal half goes), the Data Hierarchy line, "Builder flows → Nutrition" (the inputs keyed on Starts on), "Nutrition plan versions" (the goal covering the plan's effective date), "Coach client Overview" (the goals row and the New-goal line); CONVENTIONS §8 "Status-based lifecycle" gains goals beside training and nutrition, and "Multi-table writes are atomic" names the goal RPC; TECHNICAL-DEBT closes the `primary_goal` and the `updateGoals` atomicity entries. This plan: the §4 row.
- Browser smoke: set a goal on the details sheet with Starts on today and read it on the Overview band, the goal strip and the client app; save the sheet with the goal untouched and confirm the history popover gains nothing; queue a goal three weeks out and confirm the Overview shows the current goal with "New goal from", the client app shows the current goal, and the history is unchanged; remove the queued goal; open the nutrition drawer, move Starts on into the queued goal's window and watch the calories and the Goal line change, then save and read the calendar from that day; submit a check-in on the day a goal starts and confirm the review judges against the new goal; change only the deadline; open a past block whose window spans a goal change and read its goal line and pace; the block form has no target field; the client app's Program tab shows the block with the derived target.

| # | Decision | Recommendation | Why, and the alternatives |
|---|---|---|---|
| D25 | How every reader gets the goal once the copy is gone (re-decided 2026-09-04: "current" now depends on the client's calendar) | **No view, no embed.** `Client` carries no goal fields; every reader calls `getGoalForDate(clientId, day)` with the day it is asking about — the client's today for the live surfaces and the two portal routes (Shape B: the route proved the client id), the check-in's day for the review, the plan's effective date for the drawer. Mig 064's `clients_view_own_goals` already covers a client-JWT read if one is ever needed; `npm run check:rls` | The earlier recommendation, a `client_current_goals` view embedded beside the measurement views, loses: a view knows only `CURRENT_DATE` (UTC), so on the day a version starts it would show the wrong goal for the hours the client's calendar disagrees with UTC — the timezone model's one rule is whose calendar the date is on |
| D28 | The goal strip's START leg once goals are sequenced | **Agreed (owner, 2026-09-04): the reading as of the version's `effective_from`** (the baseline's own as-of rule: the latest live row on or before it, else the earliest after), read by the review beside its two other as-of reads; the Overview's "Since start" figures keep the client's origin | Today the start leg is the client's origin, and `calculateGoalProgress` takes direction from `sign(goal − start)`: a client who cut from 90 to 80 and then sets a bulk to 85 reads "overshot" on the strip from day one, because the origin sits above the new target. A goal's progress runs from where the client was when the goal began; lifetime progress runs from the origin — two questions, two starts, both derived from the log, neither stored. Alternative: keep the origin for both and accept the wrong direction on every goal that reverses course |
| D29 | Cancelling a queued goal | **Agreed (owner, 2026-09-04): a Remove action on the Overview's "New goal from" line** — `DELETE /api/clients/[id]/goals/next`, hard-deleting the never-effective version and reopening the covering one (`effective_until = NULL`), audited `goal.delete` | Alternative: re-save the current numbers from today — the sheet seeds from the open (queued) version, so the coach retypes what is already current, and the save closes the current version at yesterday and mints an identical one from today: a phantom in the history |

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commit 8d
before planning. Plan for my review before writing anything.

A goal lives in ONE place and resolves by date (my decisions, 2026-09-03 and
2026-09-04): client_goals becomes date-ranged like nutrition_plans since migration 144
— effective_from a DATE on the client's calendar, effective_until closed at the
successor's start, close-and-insert with the absorb rule, one open row, no overlaps, no
version starting before the client's today, replace only for a version that has
governed days. A goal may be queued to start on a future day and is invisible to every
reader until then: the Overview, the Journey, the client wires and the intake sync read
the version covering the client's today; the check-in review judges against the version
covering the check-in's day (winner takes all, a goal starting that day included); the
nutrition drawer reads the version covering the day the plan takes effect. A client never
sees a queued goal. The two goal columns copied onto clients and the never-mapped
goal_deadline are dropped; setting a goal is one all-or-nothing write through an RPC that
merges, checks the two date rules and writes nothing when nothing changed; the mirror
legs go; Client carries no goal fields; every client wire keeps its shape. The goal's
shape — one version, a weight target and a body-fat target, one deadline — does not
change. A block stores no target of its own: it reads the goal version covering it, and
client_phases.target_weight goes. Where ARCHITECTURE or CONVENTIONS state a rule that
contradicts this commit, do not silently follow it and do not silently override it: list
each contradiction with the
doc line and what the plan says instead, and I will review before you write. Read my
answers to D25, D28 and D29; if any is blank, stop and ask.

Job: Commit 8d of docs/MEASUREMENT-LOG-PLAN.md §6, in order: `feat(goals): a goal lives
once and resolves by date`, then `feat(nutrition): the drawer reads the goal for the day
the plan takes effect`, then `chore(blocks): the block target goes`. Build exactly what that section lists: the probe on BOTH
databases first; the migration (the date columns and their backfill, the same-day
collapse, the constraint and the exclusion, the RPC, the four column drops); the three
reads and every caller on them; the writer on the RPC; the sheet's Starts on and the
Overview's New-goal line and Remove; the review by the check-in's day; the deletions;
the tests including the request-level ones and their mutations; the wire proofs; the
docs. Then the calc-inputs route and hook, the orchestrator and the drift banner on the
plan's own date, the tests. Then the block-target migration, the block card and the
journey wire on the goal version covering the block, the deletions, the tests, the docs. Grep at execution time for every reader of getCurrentGoals,
getGoalAsOf, getGoalsHistory, updateGoals, superseded_at, goal_weight,
goal_body_fat_percentage, goal_deadline, goalWeight, goalBodyFatPercentage, primary_goal,
calcInputs, target_weight, targetWeightKg and derivePace; do not trust the list.

Migration rules as in commit 2, DEV first. Before the PROD push: `supabase migration
list --linked` against the prod ref; show me the output and wait. Rules: every fixture
number distinct; cp backups before mutating, never git stash or git checkout --; the RN
wire is additive-only; gates: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip, npm run check:service-key, npm run check:rls. Commit directly to
main, one commit each. Then replace this entry's STATUS line with SHIPPED, all three
hashes and the date, and hand me a browser smokelist: the items in the entry.
```

### Commit 9 — `refactor(journey): the Wellness pane reads its series — the merge, the check-in pager and the dialog's wellness keys go`

**STATUS: NOT STARTED. After commit 7. Revised 2026-09-03: the series has ONE source, the client's log, so the pane shows no coach entry and its log rows carry no source and no actions; the Log-measurement dialog loses its five wellness keys HERE, so that no writer of `client_metric_entries` survives this commit and commit 10 removes a dead store. Independent of commits 4–6 in code — grep at execution time, since 6 touches the same log section and 4 the same list.**

- `components/clients/metrics/hooks/use-merged-metrics.ts`: the Wellness pane's points come from `useWellnessSeries` through the same `seriesPoints` the Physique pane uses (a wellness point carries no source; the seam fills the point shape's `source` with `client_log` and `note` with null until the shapes are unified — grep at execution time whether `MetricPoint` still needs them); `useAllClientCheckIns`, `useMetricEntries` and `buildMetricPoints` leave the hook; `isLoading` / `isError` are the two series; `logMeasurement` handles physique keys only and invalidates the measurement area, with `onClientUpdated` for weight and body fat as now. Wellness `LogRow`s are one per day already (the series is day-values), with no source column, no note and no row action — the client's log is theirs; the "Before start" split stays physique-only (D20).
- The dialog (`log-measurement-dialog.tsx`): physique keys only. `METRIC_ENTRY_KEYS` narrows to `MEASUREMENT_KEYS` and `MetricEntryKey` to `MeasurementKey` (the zod enum in `lib/validations/metric-entries.ts` then refuses a wellness key on the wire — no writer survives); the wellness rows of `METRIC_VALUE_RANGES` and `METRIC_ENTRY_CONVERSION` go; `DOWN_IS_GOOD` keeps stress and soreness under `MeasurementKey | WellnessKey` for the Journey's derived stats. The Wellness pane's rail offers no "Log measurement".
- Deleted, each grepped at execution time for a surviving caller: `useAllClientCheckIns` (`hooks/use-check-in-data.ts`) and its tests — the Check-ins tab's `useClientCheckInsInfinite` and `buildCheckInsPageKey` stay; `hooks/use-metric-entries.ts`; the `GET` handler of `app/api/clients/[id]/metric-entries/route.ts` (the POST stays until commit 10 renames it); `buildMetricPoints`, `MetricSeriesDefinition` and their tests in `utils/metric-points.ts` (the module keeps `MetricPoint`, `dayValuesToMetricPoints` and the three date helpers); `WellnessMetricDefinition.key` — with no check-in field left to name, the body/wellness split of `MetricDefinition` (`use-metrics-data.ts`) collapses back to one definition shape whose wellness ids are `WellnessKey`, and the `keyof CheckIn` import goes with it. `npx knip` names the rest.
- Tests, each with a mutation: the hook over two mocked series — the Wellness pane's rows and summaries come from the wellness series and never from a check-in or an entry; a scan in the shape of `lib/measurements/baseline-ownership.test.ts` — nothing under `components/clients/metrics/**` reads `/api/clients/[id]/check-ins` or `/metric-entries` for a series (mutation: reintroduce the check-in read); the dialog offers the seven physique keys and no wellness key (mutation: twelve); the log section's wellness rows show no action and no source (mutation: the Edit action on a wellness row). `npx knip` exits clean.
- Docs, current shape only: ARCHITECTURE → the Journey row of "Client page tab structure" (both panes read a series route; the wellness series is the client's daily logs, one value per day; the dialog logs physique keys only); "client_metric_entries table" loses its merge bullet (commit 10 deletes the section); "Daily Logs" (the Journey's Wellness pane joins the domain-specific readers of `wellness_logs`). TECHNICAL-DEBT → the "Coach-logged wellness has a store of its own" item loses its pager half (commit 10 closes the rest).
- **Scope rule.** The files this commit may touch: `components/clients/metrics/**`, `hooks/use-check-in-data.ts` (the pager only), `hooks/use-metric-entries.ts`, `hooks/use-wellness-series.ts`, `app/api/clients/[id]/metric-entries/route.ts` (the GET only), `lib/metrics/metric-entry-definitions.ts` (the key narrowing), `lib/validations/metric-entries.ts` (follows the narrowing), `utils/metric-points.ts` and its test, the two docs, and whatever `npx knip` names as orphaned by those. A diff touching any reader in commit 7's blast-radius table is a defect, not a sweep item. The STATUS line lists the files touched, so the rule is verified rather than assumed.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commits 7
and 9 before planning. Plan for my review before writing anything.

Commit 7 shipped the wellness series route (see its STATUS line). This commit points the
Journey's Wellness pane at it and deletes what the client-side merge needed: the
whole-history check-in pager, the entries hook and its GET, the merge and the check-in
field on the wellness definitions; and it takes the five wellness keys out of the
Log-measurement dialog and off the wire, because a wellness score is the client's
self-report (D18 reversed) — commit 10 then removes the dead store. Where ARCHITECTURE or
CONVENTIONS state a rule that contradicts this commit, do not silently follow it and do
not silently override it: list each contradiction with the doc line and what the plan
says instead, and I will review before you write.

Job: Commit 9 of docs/MEASUREMENT-LOG-PLAN.md §6 — `refactor(journey): the Wellness pane
reads its series — the merge, the check-in pager and the dialog's wellness keys go`.
Build exactly what that section lists: the hook on two series; the dialog and the key
list narrowed to physique; the deletions, each grepped at execution time for a surviving
caller — never taken from this plan's list; the tests, the scan and their mutations; npx
knip clean; the docs. Everything you delete must be grepped first. The scope rule is a
hard boundary: re-grep commit 7's blast-radius table before you start, and touch no
reader in it.

Rules: every fixture number distinct; cp backups before mutating, never git stash or git
checkout --; gates: npx tsc --noEmit, npx eslint ., npx vitest run, npm run
check:labels, npx knip, npm run check:service-key. Commit directly to main. Then replace
this commit's STATUS line with SHIPPED, the hash and the date, and hand me a browser
smokelist: the Wellness pane for a client with daily logs (the chart's density, the hero's
week comparison), its log rows without actions, the dialog's seven keys, and the Physique
pane unchanged.
```

### Commit 10 — `chore(wellness): coach wellness logging retired — the entries table, its writer and its readers go`

**STATUS: NOT STARTED. After commit 9, which already stops every writer. Owner decision 2026-09-03, reversing D18: a wellness score is the client's self-report. One migration, DEV then PROD. D22 answered 2026-09-03, as recommended: the writer is renamed.**

- PROD probe first (CONVENTIONS §8: a destructive change re-probes PROD): `client_metric_entries` row count per client on PROD and on DEV (DEV: 2 rows, both the owner's, 29 Aug 2026), printed in the commit body; the rows go with the table.
- Migration (the next free number at execution time — commit 8 and the training-completion plan also take numbers): `DROP TABLE IF EXISTS public.client_metric_entries` (its unique constraint, CHECK and index go with it). `gen types`; diff. PROD after `migration list --linked`; `npm run check:rls` on both (a table fewer).
- Code deleted, each grepped at execution time: the wellness branch of `services/metric-entries-service.ts` and `listMetricEntries` (dead since commit 9); the activity feed's wellness-entry rows (`services/client-activity-feed-service.ts`); `AUDIT_ACTIONS`' `metric_entry.upsert`; the entry shapes of `types/metric-entries.ts`; the seeds' and fixtures' entry rows (grep `client_metric_entries` under `scripts/` and `__tests__/`). What remains of the "metric entries" family is the Log-measurement dialog's physique writer, `upsertMetricEntry` → `appendMeasurements`, and D22 decides its name.
- Docs, current shape only: ARCHITECTURE → the "client_metric_entries table" section and its line in the Data Hierarchy go; the Journey row's dialog clause; the audit call-site list. TECHNICAL-DEBT → "No DELETE/edit path for client_metric_entries" and "Coach-logged wellness has a store of its own" close by deletion.
- `npx knip` exits clean.

| # | Decision | Recommendation | Why, and the alternatives |
|---|---|---|---|
| D22 | The name of the dialog's remaining writer | **Rename: `POST /api/clients/[id]/measurements`**, beside the `…/measurements/[measurementId]/{correct,void,restore}` routes, calling `appendMeasurements` directly with `source: 'coach_entry'` and the same chain and audit (`measurement.create`); `metric-entries-service.ts`, `lib/validations/metric-entries.ts` and `types/metric-entries.ts` go; the dialog and `use-merged-metrics`' `logMeasurement` post to it | No "entries" concept survives this commit, and the route joins the family every other measurement write already lives in. Alternative: keep the `metric-entries` name for a physique-only writer — a name for a concept that no longer exists |

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commits 9
and 10 before planning. Plan for my review before writing anything.

Commit 9 shipped: no surface and no wire accepts a coach-logged wellness entry any more.
This commit removes the dead store — client_metric_entries, its writer's wellness branch,
its readers, its audit action — and, per my answer to D22, renames the dialog's remaining
physique writer into the measurements family. Where ARCHITECTURE or CONVENTIONS state a
rule that contradicts this commit, do not silently follow it and do not silently
override it: list each contradiction with the doc line and what the plan says instead,
and I will review before you write. If D22 is blank, stop and ask.

Job: Commit 10 of docs/MEASUREMENT-LOG-PLAN.md §6 — `chore(wellness): coach wellness
logging retired — the entries table, its writer and its readers go`. Build exactly what
that section lists: the PROD and DEV row probes first; the migration; the deletions, each
grepped at execution time — never taken from this plan's list; D22; the docs; npx knip
clean.

Migration rules as in commit 2, DEV first. Before the PROD push: `supabase migration
list --linked` against the prod ref; show me the output and wait. Gates: npx tsc
--noEmit, npx eslint ., npx vitest run, npm run check:labels, npx knip, npm run
check:service-key, npm run check:rls. Commit directly to main. Then replace this
commit's STATUS line with SHIPPED, the hash and the date, and hand me a browser
smokelist: the Log-measurement dialog logging a weight and a girth, the Overview's
"Since your last visit" after it, and the Journey's Wellness pane unchanged.
```

### Commit 11 — `feat(client): the client's wellness series reads their daily logs — one kernel for both apps`

**STATUS: NOT STARTED. After commit 7 (the kernel); independent of 8, 9 and 10. Owner decision 2026-09-03: the client's progress read builds its five wellness histories the way it builds the physique ones — from the store, through the shared kernel — so both apps show the same number for the same day, and the check-ins' stored averages mean one thing everywhere: what this check-in reported. No migration: the live catalog carries `clients_select_own_wellness_logs` (probed on DEV 2026-09-03).**

What is there today, by grep (2026-09-03): `services/client-portal-progress.ts` builds `wellnessMetrics` and the five `*History` arrays from `check_ins` — one point per check-in, the stored weekly average, dated `created_at` sliced to a UTC day — so a client who logs daily sees about four points a month on their own Metrics screen while, after commit 9, their coach sees every day. The physique half reads `client_measurements_live` under the client's session token through `dayValues`, the same kernel as the coach's series, and is already at parity.

- `services/client-portal-progress.ts`: the five histories from a paged read of the client's own `wellness_logs` over the route's window (`date >= fromDay`, on the client's calendar), under the client's session token, through `wellnessDayValues`; the check-ins read shrinks to the count it still serves (`checkInCount`). `wellnessMetrics` and the five history arrays keep their shape (`buildMetricSeries`, trend, percent change); their values and density change by design.
- Wire proof with the request-level harness for the fixture client and Sam Kalepa: `GET /api/client/progress` shape-compared (D6's rule — values change by design); `GET /api/client/check-ins` and `…/[id]` byte-identical, untouched.
- Tests, each with a mutation: the histories come from `wellness_logs` and never from a check-in (mutation: read the check-in averages); a null column yields no point; the window applies (mutation: drop the date bound); a scan in the shape of `lib/measurements/baseline-ownership.test.ts` — nothing under `services/client-portal*` or `app/api/client/**` builds a wellness series from `check_ins` (mutation: reintroduce it).
- Docs, current shape only: ARCHITECTURE → "Client Portal Architecture" (the Metrics hub's wellness series is the client's daily logs, one value per day, through the shared kernel; the physique and wellness series are derived the same way in both apps); the RN reference (`CLIENT-APP-REFERENCE.md`, if it holds the progress shape — grep): the wire's shape is unchanged, its wellness values are daily.
- No browser smoke beyond the harness proof: the web harness renders the same component with more points.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commits 7
and 11 before planning. Plan for my review before writing anything.

Commit 7 shipped the wellness kernel (lib/wellness/day-values.ts) with one caller, the
coach's series. The client's progress read still builds its wellness series from the
check-ins' stored weekly averages, so the client and the coach read the same logs at
different grains. This commit gives the kernel its second caller: the client's read
builds its five wellness histories from their own daily logs, the way its physique
histories already come from the measurement log. The RN wire keeps its shape; its values
change by design, as D6 allowed for physique. Where ARCHITECTURE or CONVENTIONS state a
rule that contradicts this commit, do not silently follow it and do not silently
override it: list each contradiction with the doc line and what the plan says instead,
and I will review before you write.

Job: Commit 11 of docs/MEASUREMENT-LOG-PLAN.md §6 — `feat(client): the client's wellness
series reads their daily logs — one kernel for both apps`. Build exactly what that
section lists: the progress read through the kernel, windowed, under the client's own
token; the wire proofs with the request-level harness, recorded before the switch and
diffed after; the tests, the scan and their mutations; the docs. No migration, no policy.

Rules: the RN app is the real client — its routes are the contract; every fixture number
distinct; cp backups before mutating, never git stash or git checkout --; gates: npx tsc
--noEmit, npx eslint ., npx vitest run, npm run check:labels, npx knip, npm run
check:service-key. Commit directly to main. Then replace this commit's STATUS line with
SHIPPED, the hash and the date. No browser smoke: the harness proof is the smoke.
```

### Commit 12 — `plan(logs): flatten the day spine — a plan, not a build`

**STATUS: NOT STARTED. The LAST commit (renumbered from 6 to 9 on 2026-09-03, from 9 to 11 later that day when the coach-wellness retirement and the client's series were inserted, and from 11 to 12 when commit 8, a reading edited in place, was inserted): a planning step whose output is a plan document, not code. After commits 6–11, before real clients — it ends by deleting this document, so nothing may run after it.**

- Write `docs/DAY-SPINE-FLATTEN-PLAN.md` in the shape of this document. The target: every client log is its own table found by client and date — wellness, nutrition, habits, workouts, measurements — with no parent row; the day note moves to its own table `daily_notes (client_id, date, body)`; `daily_logs`, its `daily_log_id` foreign keys, the `daily_logs_full` view (migrations 056 and 123) and the unused `upsert_daily_log_atomic` RPC go; the per-card writers drop their ensure-spine step; the attention feed and the check-in context read the domain tables joined by client and date.
- Blast radius by grep at planning time: every reader of `daily_logs`, `daily_logs_full` and `daily_log_id`; the flat `DailyLog` type (unchanged on the wire); the RN contract's per-card endpoints (`PATCH /api/client/daily-logs/[date]/*` and their GETs — byte-identical, proven with the request-level harness); RLS for the new notes table (deny-all, `service_role` grant); `check:rls`, and `check:prerender` if a page's structure moves.
- Decisions for the owner in its §3: where the day note lives; whether the dead `training_logs` table is dropped in the same migration, with its two remaining readers (`utils/daily-logs-aggregation.ts:72` and the mappers) moved to `training_events`; the DEV-then-PROD order.
- Deliverable: the document, reviewed by the owner. Nothing else changes. Then this plan document is deleted (delete-then-document) — **confirm with the owner first**.

```text
Read CONVENTIONS.md, docs/ARCHITECTURE.md and docs/MEASUREMENT-LOG-PLAN.md §6 commit 12
before planning. This is a planning step: do not write code, do not touch a migration.

Job: produce docs/DAY-SPINE-FLATTEN-PLAN.md in the shape of docs/MEASUREMENT-LOG-PLAN.md
— why the shape changes, the target shape, the decisions for me with alternatives and
your recommendation, the blast radius verified by grep, the verification, and the
commits with pasteable prompts. The target is in commit 12's bullets: every client log
its own table found by client and date, the day note in its own table, the spine and
its view gone, the per-card client endpoints byte-identical. Where ARCHITECTURE or
CONVENTIONS describe the spine as the model, list each place the new document must
rewrite. Show me the document and wait; then, only after I confirm, delete
docs/MEASUREMENT-LOG-PLAN.md and update my memory record for the measurement-log
workstream to SHIPPED with every hash.
```

## 7. Smoke setup on DEV — data the owner asked for (not commits)

Run before smoking R6 items 9 and 10 on client `ed5cb82c-30ea-488d-96d8-eb34e8ae09fa` (Samuel James, weekly, due 30 Aug, so a check-in submitted this week covers 24–30 August; its only nutrition entry is Wednesday 26).

```sql
-- 9. No nutrition logged: save the row first, then delete it.
SELECT * FROM nutrition_logs WHERE id = 'c8444f02-8741-4871-bcde-6de83e0eeca5';   -- keep this output
DELETE FROM nutrition_logs WHERE id = 'c8444f02-8741-4871-bcde-6de83e0eeca5';

-- 10. A habit added mid-period: leading dashes for Mon–Wed, dots from Thu 27 Aug.
INSERT INTO daily_habits (coach_id, client_id, name, is_boolean, sort_order, effective_date)
VALUES ('659330a8-eea6-4090-807c-69836dd2938d', 'ed5cb82c-30ea-488d-96d8-eb34e8ae09fa',
        'Stretch 10 min', true, 2, '2026-08-27');
```

Both run with `npx supabase db query "<sql>" --linked` (DEV is the linked ref). Neither table is touched by this plan.
